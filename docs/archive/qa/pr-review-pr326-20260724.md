PR REVIEW — PR #326 "feat(notifications): email customer confirmations + unmatched-payment notices" @ a55c3c6b
─────────────────────────────
Base: fix/bank-transfer-reconcile-orphan (#324, stacked) · Diff scope: 10 files, +280/-29, 1 commit
PR exists: yes · State: OPEN (not draft)

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  [BODY ACCURACY / DIFF HYGIENE] lib/config/env.ts adds a SECOND, byte-for-byte
  duplicate `superRefine` check for `EMAIL_PROVIDER === 'resend' && !RESEND_API_KEY`
  (new block at lines 373-379) when an identical check already existed at lines
  515-522 in the SAME superRefine callback (same condition, same `path`, same
  message string). Both the commit message and PR body describe this as if it were
  new wiring ("env.ts requires the key at boot when EMAIL_PROVIDER=resend") — it
  was already enforced. Functionally harmless (Zod just reports the same issue
  twice on boot failure), but it's dead/redundant code that a `grep RESEND_API_KEY
  lib/config/env.ts` before writing would have caught — the repo's own Mistake Log
  culture (CLAUDE.md 2026-06-21 entries) is built around exactly this "grep before
  adding" discipline.
    Fix: delete the newly-added block (lines 373-379) and rely on the pre-existing
    one at 515-522; adjust the commit-message clause claiming this is new.

PRIORITY 3 — Address when convenient:
  [COMMIT MSG] Subject "feat(notifications): email customer confirmations +
  unmatched-payment notices" is 77 chars (limit 72 in this checklist).
  [PR DESC] Title is the same 77-char string (limit 70).
  [PR DESC] Body uses `## Why` / `## What` / `## Tests` instead of the canonical
  `## Summary` / `## Test plan` headers. Content-wise both are covered thoroughly
  (Why≈Summary, Tests≈Test plan) — this is a heading-naming nit only, not a gap.
  [STACKED-PR HYGIENE] Body correctly flags the stack ("Stacked on #324... Rebase
  onto master after #324 lands") but doesn't call out that `lib/jobs/
  reconcilePayments.ts` is heavily edited by BOTH #324 and #326 — the single file
  most likely to conflict on rebase. Not blocking (rebase conflicts are routine),
  but worth a one-line heads-up for whoever does the rebase.
  [DOCS] `.env.example` still shows the placeholder domain
  (`EMAIL_FROM="noreply@busbookvn.com"`) rather than the go-live domain used
  throughout this PR's guide (`noreply@lenxevn.com`). Pre-existing, not introduced
  by this PR, but the PR is the natural place to have refreshed it since it's the
  one making the domain real.

Negative-space audit — all clear:
  - No schema/migration needed: `buyerEmail` (nullable, Issue 042) and
    `NotificationChannel.email` both pre-date this PR; confirmed no new
    Prisma model/column/index in the diff.
  - `EMAIL_PROVIDER` / `RESEND_API_KEY` / `EMAIL_FROM` were already declared in
    `lib/config/env.ts` and `.env.example` (commented) before this PR — no new env
    surface, just a boot-time gate + a runtime gate that were already wired
    elsewhere; genuinely net-new is only the `superRefine` duplication above.
  - `@@unique([bookingId, template])` on `NotificationLog` (pre-existing) +
    `enqueuePendingNotification`'s try/catch swallow (pre-existing, in
    reconcilePayments.ts, not part of this diff) together back the PR's
    "fires exactly once across 15-min ticks" claim — verified true, not just
    asserted.
  - Tests: 3 new `renderTemplate` cases (incl. the "no MoMo" guard), unit
    assertions for both the held-branch (`customerPaymentReview` +
    `opsUnmatchedPayment`) and lapse-branch (`customerPaymentUnverified`, and an
    explicit `not.toContain('customerBookingExpired')` regression guard against
    the false-expiry bug), plus an integration assertion on real
    `NotificationLog` rows/recipients. Solid coverage for the new surface.
  - Operator-notification-still-SMS: confirmed by diff inspection —
    `processWebhook.ts`'s `operatorNewBooking` / `operatorRecipient` block (line
    ~359-411) is untouched by this diff; the PR body's "Not in scope" claim is
    accurate, not a scope leak.
  - MoMo-copy fix: touches the same `esms.ts` `renderTemplate` switch this PR is
    already modifying to add 3 new cases, and is explained in both body and
    commit ("MoMo isn't in use; bank transfer is the live rail") — in-scope, not
    drift.
  - Rollback: no irreversible ops in the diff (no schema drops, no payment
    mutations) — this is a notification-channel toggle. `EMAIL_PROVIDER` flip
    back to unset/`stub` is a true one-line kill switch, correctly described.
  - No new runtime dependency, no new cron, no new public API route.

SUMMARY: 0 P1, 1 P2, 5 P3

RECOMMENDED NEXT STEPS:
  → Delete the duplicate RESEND_API_KEY superRefine block before merge (P2);
    trivial, no test impact expected (same ZodError path either way, just one
    issue instead of two).
  → P3s are optional polish — commit/PR title trim, header renaming, .env.example
    domain refresh, one-line rebase-conflict heads-up. None block merge.
  → No `/commit-split` or `/migration-author` follow-up needed — shape is clean
    and negative-space audit found no gaps beyond the P2 above.
