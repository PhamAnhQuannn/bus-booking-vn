PR REVIEW — PR #345 "fix(lint): make import-x/no-cycle gate actually detect cycles (#333)" @ d6603e5b
─────────────────────────────
Diff scope: 2 files, +27 / -8 lines, 1 commit
Base: `master` · Head: `fix/333-no-cycle-resolver`
PR exists: yes · State: open, ready (not draft) · Mergeable: CLEAN
Mode: PR (standalone — no auto-comment)

## Shape verdict

The mechanical shape of this PR is **excellent** and needs no restructuring. Findings below are
all negative-space (what is missing around the diff), not scope or size problems.

What is already right:
- **Scope**: one intent — `fix(lint)` — plus a CLAUDE.md mistake-log entry documenting that same
  fix. Single coherent thread; nothing to split.
- **Size**: 27/8 lines across 2 files. Far inside every threshold.
- **Commit quality**: one commit, conventional form, subject 68 chars (≤72), body present and
  explains WHY (both config gaps + the 11-cycle consequence) rather than WHAT. No wip/fixup/squash
  debris. This is the ideal single-commit PR.
- **Issue linkage**: `Closes #333` and `Follow-up: #343` — both directions present.
- **Dependency hygiene**: the new `import { createTypeScriptImportResolver } from
  "eslint-import-resolver-typescript"` is backed by an existing **direct devDependency**
  (`^4.4.5`), so there is no phantom dep and no `package.json` change is owed. Verified.
- **Rollback**: no irreversible operation in the diff (no migration, no payment mutation, no
  deletion). `git revert` is a complete and safe undo.

---

PRIORITY 1 — Block push, fix first:

  (none from a shape perspective)

  Note: `/code-review` raised a P1 on the *semantics* of the severity demotion (gate detects but
  cannot block, because `lint` is bare `eslint` with no `--max-warnings`). That is a code finding,
  not a shape finding, and is deliberately not double-counted here. It does however drive P2-3 below.

PRIORITY 2 — Fix before merge:

  [NEGATIVE SPACE / SPEC DRIFT] 5 spec documents assert the state this PR changes; none updated.
    This repo's specs are normative and read by go-live gates. All of the following now describe a
    configuration that no longer exists:

      documentation/scaffolding-infra/SI-004-linting-formatting/README.md:22,73
        `import/no-cycle` severity documented as **error** (twice).
      documentation/scaffolding-infra/SI-001-project-scaffold/README.md:161
        severity column reads **Error**.
      documentation/scaffolding-infra/SI-003-ci-cd-pipeline/README.md:57
        "circular imports across module boundaries are **blocked**" — materially false after this
        PR: 11 cycles exist and CI exits 0.
      documentation/architecture-decisions/ADR-016-module-boundaries/README.md:113
        "**Cycle-free** — `import/no-cycle` prevents the mutual-import coupling that makes domains
        inseparable" — the PR proves the codebase is not cycle-free.
      documentation/hardening/HD-004-barrel-import-hygiene/README.md:33
        `- [ ] eslint-plugin-import-x no-cycle rule at error severity (not warn)` — an explicit
        hardening checklist item that this PR moves *away* from. It is the one doc that already
        anticipated this exact regression, and it is left unticked and unannotated.

    This is the documentation-drift class the CLAUDE.md 2026-07-24 entry made a blocking concern:
    on a squash-merge the corrected understanding lives only in the commit message while the specs
    keep asserting the old, now-false state.
    Fix: annotate SI-003 / ADR-016 with the true interim state (cycles detected, not blocked,
    tracked by #343) and add a note under HD-004:33 pointing at #343. A one-line "as of #345:
    temporarily `warn`, 11 known cycles, #343 restores `error`" at each site is sufficient.

  [NEGATIVE SPACE / VERIFICATION] No regression guard ships with a gate-integrity fix.
    The PR's entire thesis is that a lint gate can be silently inert while looking green. After
    merge, nothing in the repo can distinguish "gate works, tree is clean" from "gate broke again,
    tree is unknown" — the two produce identical output. The `## Verify` section describes a manual
    cycle-injection that was performed once, by hand, and leaves no artifact.
    Both `import-x/extensions` and `import-x/resolver-next` are load-bearing; removing either
    reverts the gate to inert with zero signal.
    Fix: add a CI step or `pnpm` script that injects a temp 2-file cycle, asserts `import-x/no-cycle`
    appears in `--format=json` output, and fails otherwise. (Independently confirmed as a 3-command
    check during this review — it is cheap.)

  [PR DESC] `## Verify` frames a non-blocking gate as a passing result.
    The body reads: "`pnpm lint` exits 0 (11 cycles surface as warnings)." Exit 0 is presented as
    reassurance, but it is precisely the property that means new cycles cannot fail CI. The body is
    honest about the severity ("kept at `warn` (was falsely `error`) pending burn-down (#343)") but
    never states the *consequence*: between this merge and #343, cycle regressions land silently.
    That matters more than usual here — this PR is the head of an 8-PR merge train (#344, #346,
    #347, #355, #356, #357, #358, #301), all of which merge into a tree with no cycle enforcement.
    Squash-merge makes this body the permanent master commit message.
    Fix: one sentence — "Interim: cycles are reported but do NOT fail CI until #343 flips the rule
    back to `error`."

PRIORITY 3 — Address when convenient:

  [PR DESC] Body has no `## Summary` heading — it opens with an unheaded intro paragraph, then
    `## Consequence` and `## Verify`. Content is complete; only the conventional heading is absent.
    `## Verify` substantively serves as the test plan, so no separate `## Test plan` is owed.

  [DOCS] AGENTS.md not updated, though issue #333 names it as the home of the false claim.
    #333 reads: "the 'cycles at zero' Stage-3 gate **in AGENTS.md** has been passing vacuously."
    The PR corrects the equivalent claim in `eslint.config.mjs` (good — the old comment said "the
    Stage-2 sweep brought cross-domain reach-ins + cycles to zero; both rules are now hard gates")
    and adds a CLAUDE.md entry, but AGENTS.md:61 still carries "both flipped warn→error once the
    count is 0, gated by the existing `pnpm lint` in CI + pre-commit" with no note that the count
    was never actually 0. It reads as a historical decision record, so the drift is mild — but the
    issue being closed explicitly points at this file.

  [ROLLBACK] Revert is safe but silently restores an inert gate.
    `git revert` returns the config to `error` + blind = green + zero findings, which is
    indistinguishable from a healthy tree. Anyone reverting for unrelated reasons (e.g. lint runtime,
    which rises 34.8s → 46.6s) would silently re-disable cycle detection with no signal.
    Fix: one line in the body noting that a revert re-inerts the gate and should be paired with
    reopening #333.

SUMMARY: 0 P1, 3 P2, 3 P3

RECOMMENDED NEXT STEPS:
  → Shape is sound; nothing needs splitting or rewording at the commit level.
  → The two substantive P2s (spec drift, missing regression guard) are both cheap and both
    materially reduce the chance this exact bug recurs undetected.
  → The PR-body sentence about interim non-enforcement is the highest value-per-character change,
    given the merge train behind this PR and the squash-merge permanence rule.
