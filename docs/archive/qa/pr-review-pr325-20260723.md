PR REVIEW — PR #325 "docs: reconcile documentation/ vs docs/, drop dead FPT path, commit #320-#323 QA reports" @ 7ce7ad1
─────────────────────────────
Diff scope: 92 files, +403 / -2073 lines, 3 commits
PR exists: yes
State: open (not draft)
Base: master · Head: chore/docs-reconcile @ 7ce7ad163cdb28325f1d3538cd25c1a5bb9a70e9

Verified independently (not taken on the PR body's word):
- `gh pr diff 325 --name-only` — every path is under `documentation/` or `docs/`. Zero code files touched. Confirmed.
- File-type breakdown: 74 RENAMED (72 with 0/0 delta — pure `git mv`; 2 carry small link-fix diffs), 11 ADDED (QA reports, 391 lines total), 4 DELETED, 3 MODIFIED. The `-2073` stat is dominated by one 2054-line pure deletion (`project-structure.txt`) and 74 zero-content renames — actual human-reviewable diff is ~20 files / ~430 lines.
- Grepped current tree (`CLAUDE.md`, `AGENTS.md`, `documentation/README.md`, `.github/`, `.gitignore`, `.claude/`, `package.json`) for `documentation/current-status`, `documentation/working-track`, `documentation/project structure` — zero hits outside the files the PR itself touches (plus one pre-existing, deliberately-untouched historical reference in `docs/qa/code-review-pr125-20260623.md`). CLAUDE.md/AGENTS.md describe `documentation/` as the 7 prefix series + `business/` and never mention the two moved folders — the move makes that description *more* accurate, not stale.
- Spot-checked repaired links: `docs/current-status/comparison/README.md` and `docs/working-track/README.md` both now point `../../documentation/go-live/GL-006-phase1-launch-scope/README.md` (3/2 levels up respectively) — target confirmed to exist on disk.
- Confirmed the "ADR-020 contains no D11" claim: `grep D11` across `documentation/architecture-decisions/ADR-020-deployment*` returns nothing, while the 3 deleted stub runbooks both cite "ADR-020 D11" in their banners — the PR body's disclosure of this pre-existing stale cross-ref is accurate, and correctly left alone (out of scope, not this PR's mess to fix).
- Confirmed the 3 deleted runbooks are in fact exactly 3-line stubs (banner + heading, no body).
- Confirmed ADR-014 diff is a 2-line blockquote insertion after the existing "Choice" line — no other text touched. Immutability intact.

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  [SCOPE] Three distinct concerns bundled into one PR under a single "docs hygiene" umbrella: (1) structural move of two folders out of the spec tree, (2) content-staleness cleanup (FPT Cloud references), (3) backfilling 11 previously-untracked QA artifacts from an unrelated PR chain (#320-#323). All three commits are `docs:`-typed (not 3+ unrelated commit *types*, so this doesn't hit the hard P1 bar), and each commit is individually atomic, well-described, and independently `git revert`-able — which meaningfully de-risks the bundling. But concern (3) in particular shares no real thread with (1)/(2): it isn't about reconciling `documentation/` vs. `docs/` at all, it's catching up on unrelated committed-artifact hygiene. A reviewer bisecting a future "why did this QA report change" question has to wade through an unrelated folder-rename diff to get there.
    Fix: not worth re-splitting after the fact given each commit already reverts cleanly, but note for next time — commit 3 (`docs(qa): commit review reports for #320-#323`) would have been a cleaner standalone PR.

  [SIZE — informational, not a hard block] Raw stat (403/-2073, 92 files) nominally crosses the >800-net/>40-files P1 line, but does not warrant blocking: 74 of 92 files are zero-delta `git mv` renames (nothing to line-review), and the largest single deletion (2054 lines) is a self-described regenerable tree dump being removed wholesale, not edited. The actual reviewable surface is ~20 files / ~430 lines, which is comfortably inside the OK band. Recommend treating pure-rename files as implicitly exempt from the size gate (same spirit as the lock-file/generated-file exceptions) rather than hard-blocking on `git mv`-heavy doc PRs going forward.

PRIORITY 3 — Address when convenient:
  [PR DESC] Title is 88 chars (limit 70): "docs: reconcile documentation/ vs docs/, drop dead FPT path, commit #320-#323 QA reports". Consider shortening to "docs: reconcile documentation/ vs docs/, drop dead FPT path" and moving the QA-report detail to the body (it already is, under "What" §3).
  [PR DESC] No literal `## Summary` heading (has `## Why` instead — functionally equivalent, well-written).
  [PR DESC] No literal `## Test plan` heading (has `## Verification` instead, listing the exact greps/`ls` commands run — functionally equivalent and arguably better for a docs-only PR; no code test plan applies).

Deletions — assessed as correct, not archived:
  3 runbook stubs (3 lines each) + `project-structure.txt` (2054 lines, regenerable). All four are `git rm`, recoverable from history (`git show <sha>~1:<path>`) — deletion vs. archival is the right call; nothing here needs a live pointer for future readers, and the PR body documents the reasoning per file. No finding.

ADR immutability — assessed as correct:
  ADR-014 gets a 2-line dated supersession blockquote appended after the existing "Choice" text, not a rewrite. ADR-001/ADR-013 (FPT as rejected-alternative/market-context) are left untouched, correctly distinguishing "chosen option now defunct" from "alternative that was always rejected." This is the right treatment for an immutable decision record — flag as a positive pattern, no finding.

Negative-space audit (custom, per task focus — none of the built-in trigger-table rows apply to a docs-only diff):
  Checked whether moving 78 files out of `documentation/` requires updates to `CLAUDE.md`, `AGENTS.md`, `documentation/README.md`, `.claude/` skills, CI config, or `.gitignore`. Verified via grep (not assumed): none reference the moved paths. `documentation/README.md`'s Sections table already listed only the 7 series + `business/` + `guides/` and never listed the two strays — the PR's claim that it "needed no edit" is independently confirmed, and the move brings observed reality into alignment with what `CLAUDE.md`/`AGENTS.md`/`documentation/README.md` already assert.

Rollback path:
  Trivially `git revert` — no schema/env/payment/queue/storage mutation in the diff, no DROP/irreversible operation. Category 5 does not apply.

SUMMARY: 0 P1, 2 P2, 3 P3

RECOMMENDED NEXT STEPS:
  → No P1s — safe to merge as-is.
  → P2 SCOPE finding is retrospective (commits already atomic/revertable) — no action required before merge; carry the "split unrelated QA-artifact backfills into their own PR" lesson forward.
  → P2 SIZE finding is a false-positive-shaped raw-stat trip, not a real review burden — no action required; consider hardening the `/pr-review` size-gate exception list to exclude zero-delta renames on a future skill revision.
  → P3 PR-description nits are optional polish; the existing headings already cover the intended content.
