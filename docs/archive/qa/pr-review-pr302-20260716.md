PR REVIEW — PR #302 "feat: hero redesign, booking-journey audit fixes, brand logo, Vercel Analytics" @ ff2e2ac3
─────────────────────────────
Diff scope: 66 files (+1067 / −369, ≈661 net code lines after lockfile/binary exclusions), 4 commits
PR exists: yes
State: open (ready)

PRIORITY 1 — Block push, fix first:
  [SCOPE] 4 distinct intents in one PR: feat(analytics), feat(home), fix(ux), feat(brand).
    Detected: Vercel Analytics wiring, hero redesign, 26 audit fixes, brand-logo integration.
    Mitigation already applied: user explicitly chose single-PR (Path B) at the /ship split
    gate because 4 files (globals.css, page.tsx, layout.tsx, SearchForm.tsx) are entangled
    across waves; commits are dependency-ordered so bisect works per-wave. Treated as a
    RECORDED USER OVERRIDE — not re-blocking. Noted for the merge decision.

PRIORITY 2 — Fix before merge:
  [SIZE] ≈661 net lines across ~42 reviewable files (24 binary assets + lockfile excluded).
    Upper P2 band. Same override rationale as SCOPE; commits are the review units.
  [NEGATIVE SPACE / DEP] @vercel/analytics added to package.json; PR body has no license note.
    Fix: add license line to PR body (MIT, Vercel first-party).

PRIORITY 3 — Address when convenient:
  [PR DESC] Title 78 chars (limit 70). Fix: trim.
  [COMMIT MSG] All 4 commits conventional-format ✓ but feat/fix commits lack bodies (WHY).
    Acceptable given the PR body carries the WHY per wave.

Category notes:
  - Negative space: no schema/migration changes, no new env vars, no new app/api route
    handlers (manifest.ts / opengraph-image.tsx are Next metadata routes), no feature flags,
    no cron, no new fetch() to external hosts (VietQR is an <img> src already CSP-allowlisted).
  - Rollback: no irreversible ops. Asset deletions (app/icon.svg) revert with git. Old hero
    and sunset assets intentionally kept on disk for rollback per CREDITS.md.
  - PR body: ## Summary ✓, ## Test plan ✓, non-empty ✓.

SUMMARY: 1 P1 (user-overridden at split gate), 2 P2, 2 P3

RECOMMENDED NEXT STEPS:
  → Apply cheap fixes now: shorten title, add dep-license note to body (kills 1 P2 + 1 P3).
  → SIZE P2 rides under the recorded override.
