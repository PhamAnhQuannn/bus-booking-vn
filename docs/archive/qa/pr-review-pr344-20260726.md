PR REVIEW — PR #344 "refactor(home): remove FeatureHighlights section from homepage (#289)" @ 876add04
─────────────────────────────
Mode: PR mode (`/pr-review 344`)
Pinned head SHA: `876add04c16a5ebbde79cfa06ad3df3316c68890`
URL: https://github.com/PhamAnhQuannn/bus-booking-vn/pull/344
Base: `master` · Head: `fix/289-remove-feature-highlights`
Diff scope: 1 file, +0 / -3 lines, 1 commit
PR exists: yes
State: OPEN, ready for review (not draft) · Labels: none · reviewDecision: none yet
Mergeable: MERGEABLE · mergeStateStatus: CLEAN

---

PRIORITY 1 — Block push, fix first:

  (none)

PRIORITY 2 — Fix before merge:

  (none)

PRIORITY 3 — Address when convenient:

  [NEGATIVE SPACE / DOCS] docs/design/mockup-home-spec.md:157,161-164,268
    The home mockup spec is an *actionable build document*, and it still prescribes
    FeatureHighlights as a live homepage section to be REWORKED, not removed:
      - `:157` `### S6 — Vì sao chọn BBVN`
      - `:161` "Build — replace `FeatureHighlights`'s 3-card bento with the two-column layout:"
      - `:268` table row: "| `FeatureHighlights.tsx` | **Rework** — bento → 2-col photo+checklist …"
    Post-merge this is stale in a directional way: a future implementer working the S6 row would
    re-add a section this PR just deleted. Nothing in the PR body or the diff retires S6.
    Fix (cheap): a one-line strike-through / "superseded by #289 — section removed from homepage"
    note on the S6 row. Or leave it to the follow-up cleanup issue — but say which, so it happens.
    Not a merge blocker; PR scope explicitly excludes cleanup.

    Positive side note worth recording: `mockup-home-spec.md:164` and `:221` flag
    `'Đánh giá 4.8/5 từ hành khách'` (`FeatureHighlights.tsx:26`) as a **fabricated rating live on
    the production site**, marked "must be removed in this rework regardless of anything else".
    Removing the whole section takes that fabricated claim off the live homepage. This PR silently
    closes a standing content-integrity item — worth calling out in the PR body, as it materially
    raises the PR's value beyond "remove 3 cards".

  [PR DESC] PR body has no `## Summary` heading.
    Body opens with `Closes #289.` then an unlabelled descriptive paragraph, then `## Verify`.
    Purely a heading-convention nit — the content is present and clear.
    The `## Verify` section satisfies the `## Test plan` requirement in substance; NOT flagged
    separately. Linked issue (`Closes #289`) present. Title 69 chars (limit 70) — passes.

---

## Category-by-category walk

### Category 1 — Scope discipline
**PASS — exemplary.** One commit, one intent (`refactor`), one scope (`home`), one file.
No mixing of feat/fix/refactor, no unrelated domains, no drive-by "improvements" to adjacent code.
This is the shape `/commit-split` exists to produce. Nothing to split.

### Category 2 — Diff size
**PASS.** Net **-3** lines across **1** file — three orders of magnitude below the P2 threshold
(401–800 net / 21–40 files). No lock-file or generated-file exceptions needed. Trivially reviewable,
trivially bisectable.

### Category 3 — Commit message quality
**PASS.** Single commit:
```
refactor(home): remove FeatureHighlights section from homepage (#289)
```
- Conventional form `<type>(<scope>): <subject>` — valid type `refactor`, valid scope `home`. ✓
- 69 chars, under the 72 limit. ✓
- No `wip` / `fixup` / `asdf` / `address comments`. ✓
- Empty `messageBody`. The body requirement applies to `feat:` and `fix:` only — `refactor:` is
  exempt, and for a self-describing 3-line deletion carrying an issue ref, a body adds nothing.
  **No P3.**
- Squash-merge safety (2026-07-24 Mistake Log — "a squash-merge inherits the PR body"): single
  commit, and the PR body was written against the final and only diff. No commit-1-vs-final drift
  is possible here. The class of defect that entry warns about cannot occur on this PR.

### Category 4 — Negative-space audit (what is NOT in the diff)

| Trigger | Present in diff? | Companion required | Verdict |
|---|---|---|---|
| New column / table / index in `prisma/schema.prisma` | No | migration file | N/A |
| New env var (`process.env.X`) | No | `.env.example` entry | N/A |
| New route handler / server action | No | test file | N/A |
| New feature flag | No | rollout plan / sunset date | N/A |
| New public API endpoint (`app/api/**/route.ts`) | No | API/OpenAPI docs | N/A |
| New runtime dep (`package.json`) | No | license note | N/A |
| New cron / scheduled job | No | failure-mode + observability doc | N/A |
| New external API call | No | timeout + retry note | N/A |

Zero triggers fire. A pure-deletion UI diff touches none of the negative-space categories.

**One companion genuinely absent (P3 above):** design-doc drift in `docs/design/mockup-home-spec.md`.
Not a table row — the table has no "removed a documented UI section" trigger — but it is the real
negative space of this particular PR, so it is recorded rather than forced into a category.

Other surviving `FeatureHighlights` references were checked and are **NOT** drift:
- `documentation/frontend-design/FD-001-design-system/README.md:96` — a `components/home/` file
  inventory. The component file still exists (deliberately), so the inventory stays accurate.
- `documentation/frontend-design/FD-007-responsive-mobile/README.md:38` — cites the bento grid as a
  breakpoint example; still valid as a code reference.
- `docs/design/landing-page-scan-20260720.md`, `docs/design/landing-page-color-report-20260721.md`
  — **dated point-in-time audit snapshots.** Historical records are supposed to describe the page as
  it was on that date. Editing them would be falsifying the record. Correctly left alone.
- `public/features/CREDITS.md` — image attribution for assets deliberately retained. Still accurate.

### Category 5 — Rollback path
**PASS — and unusually clean.** Scanned for irreversible operations: no `DROP COLUMN` / `DROP TABLE`
/ `DROP INDEX`, no migration at all, no payment-processor mutation, no queue purge, no shared-storage
deletion, no `rm -rf` in any script. Nothing irreversible.

Rollback is a plain `git revert 876add04` — and it is **fully self-contained** precisely *because*
the PR deliberately did not delete `components/home/FeatureHighlights.tsx` or `public/features/*.jpg`.
A revert restores three lines and the section renders again; no asset restoration, no migration
reversal, no data backfill. Deferring the file deletion to a separate cleanup is therefore not just
AC compliance — it is what keeps this PR one-command-revertible. Good shape; no rollback doc needed.

### Category 6 — PR description completeness

| Check | Result |
|---|---|
| Title ≤ 70 chars | **PASS** — 69 |
| Body non-empty | **PASS** |
| `## Summary` section | **FAIL (P3)** — content present, heading absent |
| `## Test plan` section | **PASS in substance** — `## Verify` block lists lint / tsc / homepage check |
| Linked issue | **PASS** — `Closes #289.` |

Body also proactively states the deliberate carve-out ("Component file + `/public/features/` images
intentionally kept (separate cleanup, per the issue)"), which pre-empts the obvious reviewer
question about the orphaned component. Good practice.

### Category 7 — Negative-space audit on real PR body (PR mode only)
- New feature flag + no rollout/sunset in body → no flag in diff. **N/A.**
- Irreversible op + no rollback notes in body → no irreversible op. **N/A.**
- New runtime dep + no license note in body → no dep change. **N/A.**

All three sub-checks vacuously pass.

---

## Corroborating signals (not skill categories, recorded for the merge decision)

**CI: 12/12 green** at `876add04` —
Lint & Typecheck · Unit Tests · Integration Tests · E2E Tests (chromium) · E2E Tests (mobile-390) ·
Flaky E2E Quarantine · Data Leak Audit · Dependency Audit · Greppable Invariants ·
Secret Scanning (gitleaks) · Vercel deploy · Vercel Preview Comments.

Both E2E suites passing is the meaningful signal here: it is **empirical** confirmation that no
Playwright spec asserted on the removed section's text or testid. That closes the risk called out in
the 2026-05-19 Issue 011/012 Mistake Log entries — a stale assertion in a **sandbox-gated** spec that
rots silently and only detonates later. Combined with the static grep (`/code-review` Cat 4 — zero
hits for `FeatureHighlights` / `Vì sao chọn BBVN?` anywhere under `e2e/` or `__tests__/`), both the
static and dynamic checks agree: nothing depended on this section.

`Lint & Typecheck` green independently confirms issue #289's AC "No unused import warnings" and
"`pnpm tsc --noEmit` passes" — the import was removed in the same hunk as the render.

**Streaming-boundary check (2026-07-17 Mistake Log).** Explicitly verified per the review brief:
no `loading.tsx` added or removed anywhere in the `(customer)` segment; no `useSearchParams` added to
a client layout; the segment's only streaming boundary remains the pre-existing explicit
`<Suspense fallback={<ResultsSkeleton/>}>` scoped inside `page.tsx`, and `export const dynamic =
'force-dynamic'` is untouched. `/booking/result/[token]`'s ability to emit a real HTTP 404 via
`notFound()` is unaffected. The `momo-booking.spec.ts` e2e that guards exactly this passed in CI.

**Tailwind/Turbopack caveat (2026-07-18 Mistake Log).** Honoured: this review is source- and
CI-based. No dev-server visual inspection was treated as authoritative. Not that it could matter — a
pure deletion generates no new utility classes, so the per-file stale-rescan failure mode cannot
mask this change.

---

SUMMARY: 0 P1, 0 P2, 2 P3

RECOMMENDED NEXT STEPS:
  → **Merge-ready.** Zero P1, zero P2. Shape is close to ideal: one commit, one intent, one file,
    three deleted lines, conventional subject, linked issue, green CI, clean revert path.
  → Optional before merge (both P3, both ~1 line): add a `## Summary` heading to the body, and note
    that removing the section also takes the fabricated "Đánh giá 4.8/5" rating off the live site.
  → Follow-up, not this PR: retire the `S6 / Rework FeatureHighlights` row in
    `docs/design/mockup-home-spec.md`, and confirm the promised cleanup issue exists for
    `components/home/FeatureHighlights.tsx` + `public/features/*` + `public/features/CREDITS.md`.
    Without it, the orphaned component and its shipped image assets become permanent.
  → Per project convention (memory: no-auto-merge), do NOT merge — leave for the user.
