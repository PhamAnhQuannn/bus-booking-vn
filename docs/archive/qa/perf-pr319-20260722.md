PERF REVIEW — PR #319 "feat(landing): rebuild homepage, new hero photo, glass navbar, uppercase BBVN brand"
─────────────────────────────────
PR:        https://github.com/PhamAnhQuannn/bus-booking-vn/pull/319
Base/Head: master ← feat/landing-mockup-match @ 52a0778f
Size:      +4218 / -516 across 59 files

NOTE ON SCOPE: this review was activated by judgement, not by the skill's own
trigger. None of the documented triggers (schema / lib/db / app/api route /
new fetch) match — the diff contains no database code whatsoever. It was run
because the PR replaces the homepage LCP element and rewrites 11 brand binaries.

Findings: 1  (P1: 0 · P2: 1 · P3: 0)   — the P2 was FIXED in this pass.

Cat 1 — DB hotspots ................ CLEAN. No prisma/db call added anywhere.
Cat 2 — Memory loads ............... CLEAN. No readFileSync, arrayBuffer or
                                     unbounded JSON.parse added.
Cat 3 — Frontend bundle ............ CLEAN. Every new import is either a NAMED
                                     lucide-react icon (tree-shakeable), a local
                                     component, next/link, or react. No moment,
                                     lodash, aws-sdk, firebase or pdfjs.
Cat 4 — Cold start / always-on ..... CLEAN. No setInterval, no cron, no worker.
Cat 5 — Caching gaps ............... N/A. No new GET handler.

P2 — FIXED IN THIS PASS:
  public/brand/*.png  📦 P2: brand PNGs shipped unoptimised.
    logo-horizontal.png was 173,333 B for a 726x294 image. The header pulls the
    colour lockup and the footer pulls the white one, so EVERY page carried
    334,554 B of logo — comparable to the 431 KB hero LCP asset itself.
    Cause: the artwork is flat colour over a soft glow, and a full-depth RGBA
    PNG stores that glow as thousands of unique partial-alpha pixels.
    Fix applied: quantise to a 256-colour palette in scripts/brand-cut.py.
    Dimensions unchanged, so Logo.tsx's intrinsic sizes and the OG card's aspect
    ratio remain valid.

      logo-horizontal.png         173,333 ->  43,596 B   -75%
      logo-horizontal-white.png   161,221 ->  40,621 B   -75%
      logo-vertical.png           158,813 ->  36,159 B   -77%
      logo-vertical-white.png     156,319 ->  36,240 B   -77%
      logo-mark.png                92,492 ->  20,538 B   -78%
      logo-mark-white.png          82,928 ->  19,382 B   -77%
      icon-192.png                 44,055 ->   6,406 B   -85%
      icon-512.png                192,275 ->  25,387 B   -87%
      icon-512-maskable.png       100,650 ->   8,042 B   -92%
      apple-touch-icon.png         23,326 ->   3,159 B   -86%

      per-page logo cost          334,554 ->  84,217 B   -75%

    Quality cost measured, not assumed: mean per-channel delta 0.54, max 49,
    and only 4.32% of pixels differ by more than 8. Verified visually at render
    size and on the live header — indistinguishable.

HERO ASSET DELTA (informational — this PR IMPROVES it):
  The hero is the homepage LCP element. Only one variant downloads per viewport
  (the four preloads in app/(customer)/page.tsx are media-gated).

      landing-golden-1280.jpg      156,610 ->  186,946 B   (mobile, +30 KB)
      landing-golden-md-1536.jpg   297,851 ->  370,594 B   (+73 KB)
      landing-golden-1920.jpg      314,208 ->  426,784 B   (+113 KB)
      landing-golden-3840.jpg    1,496,734 ->  431,595 B   (-1.06 MB)

      total across variants      2,265,403 -> 1,415,919 B   -849 KB

  The 3840 variant is now exported at 2560 wide rather than upscaled to 3840,
  which is where the saving comes from. Widths below that grew modestly because
  the new master carries more scene detail.

RECOMMENDED NEXT:
  - Nothing outstanding. The single P2 is fixed and re-measured.

SUMMARY: 0 P1 · 1 P2 (fixed) · 0 P3 · pinned to 52a0778f
