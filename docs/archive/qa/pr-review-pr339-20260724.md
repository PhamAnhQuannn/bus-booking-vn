PR REVIEW — PR #339 "feat(notification): branded HTML emails — logo + ticket detail rows" @ c3778e16
─────────────────────────────
Diff scope: 4 files, +365 / -15 lines, 1 commit
PR exists: yes
State: open (ready)

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  [NEGATIVE SPACE / ENV CONSISTENCY] lib/notification/emailBody.ts:31
    Introduces a NEW env var `PUBLIC_BASE_URL` that duplicates the repo's
    existing base-URL var `NEXT_PUBLIC_BASE_URL` (lib/storage/storage.ts:160,
    behind a STORAGE_BASE_URL override). Two vars for "the public site origin"
    will drift — prod could set one and not the other. Neither is documented in
    .env.example.
    Fix: reuse `NEXT_PUBLIC_BASE_URL` in publicBaseUrl() (keep the prod-safe
    'https://lenxevn.com' default rather than storage's localhost default, since
    email links must never point at localhost); add a one-line doc to .env.example.

PRIORITY 3 — Address when convenient:
  (none)

Checked, OK:
  - Scope: single intent feat(notification) + its test — 1 concern. ✓
  - Size: 350 net lines, 4 files — well under thresholds. ✓
  - Commit: 1 commit, conventional `feat(notification): …` ≤72 chars, body explains WHY. ✓
  - Rollback: no migration, no payment/queue/storage-destructive op — nothing irreversible. ✓
  - Negative space: no new route handler (generateTicketPdfs pre-exists), no new
    dep, no schema, no feature flag. The remote logo is an <img src>, not a
    fetch() → no timeout/retry companion needed. ✓
  - PR desc: title 60 chars, ## Summary + ## Test plan present, body non-empty. ✓

SUMMARY: 0 P1, 1 P2, 0 P3

RECOMMENDED NEXT STEPS:
  → P2 (env-var reuse) is non-blocking but cheap and correct — fold into the
    review-fix commit alongside the code-review P1.
