PR REVIEW — PR #259 "feat(auth): migrate OTP to email + enable customer auth (#168, #169, #170)" @ 026729fb
─────────────────────────────
Diff scope: 60 files, +1368 / -779 lines, 5 commits
PR exists: yes
State: open

PRIORITY 1 — Block push, fix first:

  [SIZE] 60 files (+1368 / -779, 589 net excl. migration) across 59 non-generated files.
    Files > 40 threshold. However: high cohesion — all files serve the same feature
    (OTP phone→email + customer auth enable). Squash merge mitigates bisect risk.
    12 test files + 3 doc files + 1 generated migration = 16 companion files.
    Core change files: ~43. Still above 40 threshold.
    Recommendation: accept as-is given single-feature cohesion. Splitting would create
    a broken intermediate state (OTP migration without auth enable = dead code).
    Verdict: P1 per rule, WAIVE recommended — splitting not practical.

PRIORITY 2 — Fix before merge:

  (none)

PRIORITY 3 — Address when convenient:

  [PR DESC / TITLE] PR title is 73 chars (limit 70).
    "feat(auth): migrate OTP to email + enable customer auth (#168, #169, #170)"
    Fix: shorten to "feat(auth): migrate OTP to email + enable customer auth"
    (issue refs already in PR body via "Closes #168, #169, #170").

  [COMMIT MSG] Commit 4 "fix(test): remove unused hashPassword import in lockout test"
    fix: commit missing body (explains WHY). Trivial change — acceptable without body.

  [SCOPE / STRAY] 2 unrelated doc files modified:
    - documentation/business/regulatory/einvoice-tax.md (+2/-1)
    - documentation/guides/11-setup-misa.md (+38/-2)
    These are MISA e-invoice setup docs, unrelated to auth. Non-blocking — doc-only churn.

CATEGORY DETAIL:

  Cat 1 — Scope discipline:
    Intents: feat(auth) + fix(test) = 2 related intents → OK
    All 5 commits serve the same feature thread. No unrelated feat/fix domains.
    Stray docs (einvoice/MISA) are minor scope leak → P3 only.

  Cat 2 — Diff size:
    589 net lines (P2 range: 401-800), 59 files (P1 range: > 40).
    After excluding migration (1 file, +13 lines): 58 files, 576 net.
    High cohesion: single feature, splitting creates broken intermediate.
    Verdict: P1 technically, WAIVE recommended.

  Cat 3 — Commit message quality:
    5/5 conventional commit format ✓
    4/5 have body ✓ (commit 4 missing body — trivial import removal)
    0 wip/fixup/asdf ✓
    All subjects ≤ 72 chars ✓

  Cat 4 — Negative-space audit:
    ✓ New schema column (OtpAttempt.email) → migration 20260703030000 present
    ✓ .env.example updated with env var changes
    ✓ New component (CustomerAccountMenu.tsx) → test file added
    ✓ New module (clientSession.ts) → test file added
    ✓ No new route handler files (all modifications)
    ✓ No new runtime deps
    ✓ No new cron jobs
    ✓ No new external API calls

  Cat 5 — Rollback path:
    Migration is additive (ADD COLUMN email, ALTER phone nullable). Reversible.
    proxy.ts deletion (Layer 0.5) is reversible (re-add lines).
    No DROP COLUMN, DROP TABLE, or irreversible payment ops.
    ✓ Clean

  Cat 6 — PR description completeness:
    ✓ Body non-empty
    ✓ ## Summary present (7 bullet points)
    ✓ ## Test plan present (6 items, 5 checked)
    ✓ Linked issues: "Closes #168, #169, #170"
    P3: Title 73 chars (limit 70)

  Cat 7 — PR body negative-space (PR mode):
    ✓ No new feature flags → skip
    ✓ No irreversible ops → skip
    ✓ No new runtime deps → skip

SUMMARY: 1 P1 (waivable), 0 P2, 3 P3

RECOMMENDED NEXT STEPS:
  → WAIVE size P1 — single-feature cohesion, splitting impractical
  → Optionally shorten PR title before merge
  → Stray doc changes are harmless, can ride this PR
