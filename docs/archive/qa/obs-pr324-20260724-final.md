OBSERVABILITY REVIEW — PR #324 "fix(payments): reconcile sweeper could not recover ANY bank transfer (Bug B)"
──────────────────────────────────────────────────────────────────────────────────────────────────────────
PR:        https://github.com/PhamAnhQuannn/bus-booking-vn/pull/324
Base/Head: master ← fix/bank-transfer-reconcile-orphan @ 1365cc4f (final HEAD, 3 commits)
Decision:  (none)
Size:      +1032 / -77 across 16 files (cumulative, all 3 commits)
Generated: 2026-07-24T00:00:00Z
Prior round: docs/qa/obs-pr324-20260723.md (pinned to 0435fe17, commit 1 of 3) — READ, re-verified below.

Project conventions detected (unchanged from round 1):
  logger:  pino, `logger.info/warn/error({ ...fields }, 'domain.event.name')` from `lib/logger.ts`
  tracer:  none detected
  metrics: none detected (no counter.inc/histogram/statsd/prom-client anywhere in the repo). Only
           error-reporting seam: `captureException`/`captureMessage` in `lib/observability/sentry.ts`,
           which forward to `@sentry/nextjs` ONLY when `SENTRY_DSN` is set (bare `Sentry.init(...)` in
           `instrumentation.ts`, no pino transport / no log-level auto-capture integration configured).
           A direct `logger.error(...)` call — including the new one this round — does NOT reach Sentry
           under any configuration; only explicit `captureException`/`captureMessage` calls do.

Scope of this round: commits 2 (320bd7c) and 3 (1365cc4) — commit 1 (0435fe17) was already reviewed in
round 1 and is UNCHANGED at final HEAD (`git diff 0435fe1..HEAD -- lib/payment/processWebhook.ts` and
`-- app/api/payments/bank_transfer/webhook/route.ts` are both empty), so its findings are re-stated, not
re-derived.

Findings: 4  (P1: 1 · P2: 2 · P3: 1)

P1 — BLOCKING (carried over from round 1, confirmed unchanged at final HEAD):
  lib/payment/processWebhook.ts:96-134 (recordUnmatchedPaymentEvent)  🔇 P1: Still no metric/counter and
  no threshold alert on the orphan-PaymentEvent backlog.
    Re-verified against 1365cc4, not re-derived: `processWebhook.ts` has zero diff between commit 1 and
    final HEAD — commits 2 and 3 touch only `lib/jobs/reconcilePayments.ts` (+ its migration/tests) and
    never add a counter, histogram, or `captureMessage` anywhere in the PR (`git diff master...HEAD | grep
    -E 'metrics\.|counter\.|histogram\.|statsd|captureMessage'` returns only the one pre-existing
    `captureException` call on the DB-write-FAILURE branch of `recordUnmatchedPaymentEvent` — not on the
    success path that actually creates an orphan). The only per-event signal on a created orphan remains
    a `logger.warn`. Reinforcing evidence found this round: the codebase already has the exact admin
    surface this needs — `lib/admin/getFailureAlerts.ts` powers the admin Overview "Failure alerts"
    section with `failedNotifications` / `failedPayouts` counts — and `PaymentEvent` orphans are not wired
    into it, despite this being the SAME class of bug (money stuck, zero visibility) that this PR's own
    commit 1 fixed once already. Commit 2's message explicitly lists "backlog alert" under "Deferred and
    logged" — this is a named, intentional deferral, not a silent gap, which is materially better than
    round 1 found it. Verdict: finding STANDS technically (no code change addresses it), but it is no
    longer an *undisclosed* gap — it's an author-acknowledged fast-follow.
    Recommendation on merge gate: do NOT re-block this PR on it a second time — the correctness fixes in
    commits 2–3 (wrong-payee payout prevention, starvation/seat-freeze fix) are higher-severity and this
    PR is strictly safer than master with or without the metric. Condition the "accepted follow-up" status
    on a tracked issue existing for it (grep the repo's issue tracker / GitHub issues for "orphan backlog"
    before merge; file one if absent — this is item #27 on the working session's own task list). If no
    tracked issue exists at merge time, treat as P1-blocking per the 0-P1 gate.

P2 — SHOULD FIX:
  lib/jobs/reconcilePayments.ts:479 (`reconcile.unmatched_payment_unresolved`)  ⚠️ P2: The new error-level
  log is a log line only — it does not reach any alerting sink, contradicting the apparent intent behind
  escalating it.
    The commit message frames warn→error as making this "not silent" for "the one place a probably-paid
    booking gets closed" — but per the sink audit above, `logger.error(...)` (a bare pino call, not
    routed through `captureException`/`captureMessage`) never reaches Sentry regardless of `SENTRY_DSN`.
    Its only effect is local: it clears the default `LOG_LEVEL` threshold (`info` in prod) the same as
    `warn` already did, and tags the line `level:"error"` for whoever is grep'ing/dashboarding raw log
    output. Net: a booking whose money probably arrived closes with a log line nobody is paged for — same
    root cause as the P1 above, but concretely instantiated on the exact "closing probably-paid money"
    event this commit calls out as the one that must not be silent.
    Mitigating factor (not a regression): before commit 3 this event didn't exist at all — the hold was
    permanent and never resolved to any terminal log. Escalating AND bounding it is a net improvement over
    master; it just doesn't reach the bar "not silent" implies.
    Fix: since this fires at MOST ONCE per booking (see log-spam analysis below — the candidate query
    filters `status = 'awaiting_payment'` and the expiry flips status away from it, so the booking exits
    the candidate pool the same tick), pairing it with `captureMessage('reconcile.unmatched_payment_unresolved', {...})`
    is cheap on Sentry issue volume (unlike the per-tick warn, which fires up to 96×/booking and would
    flood an issue stream if captured the same way) and would actually deliver on the escalation's intent.

  lib/payment/processWebhook.ts:124-128 (recordUnmatchedPaymentEvent, P2002 branch) — carried over,
  unchanged. Duplicate/redelivery of an orphan write still returns completely silently (zero log at any
  level), inconsistent with the sibling P2002 branch in the same file (`payment.webhook.duplicate_ipn`)
  which does log. Not re-derived — see round-1 report lines 43-61 for full detail; still applicable
  verbatim since the file has zero diff since commit 1.

P3 — ADVISORY:
  lib/jobs/reconcilePayments.ts:463-473 (`reconcile.unmatched_payment_suspected`, warn, re-fires per tick)
  📊 P3: Log-spam concern is now BOUNDED, not eliminated — net improvement over the pre-commit-3 state,
  worth a follow-up throttle but not blocking.
    Before commit 3: the hold had no age check at all — the warn re-fired every 15-min tick FOREVER for
    any held booking (unbounded, matches commit 3's own "~96 lines/day each, forever" framing of the bug
    it fixes). After commit 3: `SUSPECTED_HOLD_MAX_AGE_MINUTES` (24h) caps it at ~96 lines total per
    booking, then a single terminal `error` line and the booking leaves the candidate pool (`status`
    changes away from `awaiting_payment`, confirmed via the candidate query's
    `WHERE b."status" = 'awaiting_payment'` filter at reconcilePayments.ts:284) — no further re-fires.
    This is a real, verified fix to the log-volume problem, not just a partial mitigation. Residual
    concern: 96 near-identical warn lines per held booking (fixed structured payload: bookingRef,
    paymentEventId, providerTxnId, amountVnd) is still avoidable noise during a spike of unrelated
    deposits (the PR's own stated mechanism — SePay notifies on every credit, not just ticket payments).
    Consider throttling to e.g. once/hour or once on first-detection + once at resolution, deferred as
    non-blocking polish.

VERIFIED — NOT A FINDING:
  PII in new/changed logs (task item 4). The only new log call sites added by commits 2–3 are the two
  `reconcile.unmatched_payment_suspected` (warn) / `reconcile.unmatched_payment_unresolved` (error) lines
  in `lib/jobs/reconcilePayments.ts`. Fields logged: `bookingRef`, `paymentEventId`, `providerTxnId`,
  `amountVnd`, `heldForHours`. None appear in `lib/logger.ts`'s redact-path list or
  `lib/observability/sentry.ts`'s `REDACT_KEYS` set; none are payer name / phone / raw webhook body. No
  `rawBody` reference in either new call (grepped the full `master...HEAD` diff for `rawBody` — its only
  appearance is the pre-existing, unchanged `prisma.paymentEvent.create({ data: { ..., rawBody } })` in
  `processWebhook.ts`, commented "stored for audit; never logged"). No `console.*` used anywhere in the
  diff. Consistent with round 1's PII audit, which covered the same file's other log sites.

  Log-line-fires-at-most-once claim (underpins the P2 fix recommendation above). Verified by reading the
  candidate query (`WHERE b."status" = 'awaiting_payment'`, reconcilePayments.ts:284) against the branch
  order: the error path falls through into the existing (b)/(c) expire logic in the SAME tick (the
  `continue` that previously followed the warn was removed only for the escalated branch — see
  `git show 1365cc4 -- lib/jobs/reconcilePayments.ts`), and since `SUSPECTED_HOLD_MAX_AGE_MINUTES` (24h)
  is far longer than the booking's own hold TTL (10 min per RECONCILE_THRESHOLD_MINUTES docs), by the
  time a booking crosses the 24h mark `holdExpired` is always true, so the expire UPDATE runs
  unconditionally in the same pass and the booking's status leaves `awaiting_payment` before the next
  tick. Also directly asserted by the new test (`lib/jobs/__tests__/reconcilePayments.test.ts`, "EXPIRES a
  held booking once the hold window elapses"): `tx.$executeRaw` called exactly once, `mockLogger.error`
  called with the escalated message.

  lib/jobs/reconcilePayments.ts:322-325 (`reconcile.degraded_claim_lost`, round 1's third P2) — MOOT, not
  re-flagged. Commit 2 deleted the CAS-claim code path entirely (the sweeper no longer writes to
  `PaymentEvent` at all — confirmed via `grep -n "degraded_claim_lost|CAS|executeRaw" lib/jobs/reconcilePayments.ts`,
  only the unrelated expire-UPDATE `$executeRaw` remains). The finding is resolved by removal of the code
  it was about, not by a logging fix — noting this so it isn't mistakenly counted as outstanding.

RECOMMENDED NEXT:
  - The carried P1 (orphan backlog metric/alert) does not block THIS merge provided a tracked follow-up
    issue exists (see task-list item #27, "File follow-up issues for deferrals") — this PR's net safety
    delta (fixes a live wrong-payee payout vector + an unbounded starvation/seat-freeze bug) dominates the
    unchanged observability gap. If no tracked issue exists at merge time, this reverts to P1-blocking.
  - Cheap, same-commit-sized fix available for the P2 (pair `reconcile.unmatched_payment_unresolved` with
    `captureMessage`, safe because it fires ≤1×/booking) — worth bundling into a fast-follow with the P1.
  - If reviewer already requested changes: /pr-feedback-route 324

SUMMARY: 1 P1 (accepted-deferred, conditional on a tracked issue) · 2 P2 · 1 P3 · pinned to 1365cc4f
