PR REVIEW — PR #320 "fix(payments): SePay bank-transfer go-live — Apikey auth, memo hyphen-strip, 15m window" @ 4eea40ef
─────────────────────────────
Diff scope: 12 files, +289 / -58 lines, 1 commit
PR exists: yes · State: open (non-draft)
Head SHA: 4eea40ef2a52fb3e3026656d4c422d5dd5aac3be

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  (none)

PRIORITY 3 — Address when convenient:
  [COMMIT MSG / PR DESC] Title + commit subject are 87 chars (limit 70/72).
    "fix(payments): SePay bank-transfer go-live — Apikey auth, memo hyphen-strip,
    15m window". Cosmetic; single well-scoped commit with a full explanatory body.

  [PR DESC] Body uses `## Why` / `## Verify after deploy` instead of the literal
    `## Summary` / `## Test plan` section headers. Semantically complete (rationale +
    verification steps both present) — header naming only.

SUMMARY: 0 P1, 0 P2, 2 P3

Category notes (verified clean):
  • Cat 1 Scope — single coherent theme "SePay bank-transfer go-live": fix (auth+ack+
    memo regex + window) with its supporting docs (.env.example, guide, CLAUDE.md) and
    a prod test-seed script. Not 3+ unrelated intents. OK.
  • Cat 2 Size — net +231 across 12 files (≤400 / ≤20). OK.
  • Cat 3 Commit — 1 conventional commit (`fix(payments): …`) with a WHY body. Only the
    subject-length P3 above.
  • Cat 4 Negative-space — no new env var (SEPAY_API_KEY/VIETQR_* pre-exist; .env.example
    edit is doc clarification), no new route (existing webhook modified + its test
    updated), no schema/migration, no cron, no new dep, no new external call. seed
    script is a one-off CLI (not a route) → no test-file requirement, mirrors
    purge-demo-catalog.ts. Clean.
  • Cat 5 Rollback — no irreversible op (no DROP, no payment-processor mutation in code,
    no queue purge). Seed-script DB writes are reversible pre-payment; auth/window
    changes revert via git. No rollback-plan gap.
  • Cat 6/7 PR body — non-empty, rationale + verify steps present; only the header-naming
    + title-length P3s. No feature flag, no irreversible op, no new dep to document.

RECOMMENDED NEXT STEPS:
  → No blocking findings. Safe to merge on the PR-shape axis.
  → P3s are cosmetic (title length, section header names) — ignore or tidy on a future PR.
