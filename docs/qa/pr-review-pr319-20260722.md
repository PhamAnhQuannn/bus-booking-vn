PR REVIEW — PR #319 "feat(landing): rebuild homepage, new hero photo, glass navbar, uppercase BBVN brand" @ 52a0778f
─────────────────────────────
Diff scope: 59 files (34 binary), +4218 / -516 lines, 17 commits
PR exists: yes    State: open, ready for review

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  [SIZE] Net 3702 lines total — over the >800 P1 threshold by the letter of the
    rule. Reported as P2 after decomposing where those lines live:

      app/component code   +1136  -516   net  620   (17 files)  <- reviewable runtime surface
      standalone scripts   + 790     0   net  790   ( 4 files)  <- dev tooling, never shipped
      docs / reports       +2292     0   net 2292   ( 5 files)  <- markdown
      binary assets                                  (34 files)  <- not line-reviewable

    The runtime surface is 620 net across 17 files, which sits in the P2 band
    (401-800 / 21-40). The scripts are standalone cutting/grading tools with no
    import edge into the app; the docs are design reports. Neither carries
    review risk in the way the rule is aiming at.
    Recording the disagreement rather than hiding it: by total lines this is a
    P1 and a reviewer who wants the strict reading is entitled to it.

  [SCOPE] Four intents (feat / fix / revert / chore) across five scopes
    (landing, header, hero, brand, design). Landing + header + hero are one
    continuous thread — the same page, iterated. **brand is the separable one:**
    the logo set also lands on the operator console, all nine auth routes, the
    PWA icons and the OG card, none of which are the landing page.
    Fix, if wanted: carve `feat(brand)` + its assets into a second PR.
    Disposition: the single-PR shape was chosen knowingly at plan time (the ship
    plan named 39 files / one PR and was approved), and the SePay work was
    already split out on the same pass. Left as-is, flagged so the call is
    visible rather than implicit.

PRIORITY 3 — Address when convenient:
  [PR DESC] Title is 83 chars (limit 70).
    "feat(landing): rebuild homepage, new hero photo, glass navbar, uppercase BBVN brand"

SUMMARY: 0 P1, 2 P2, 1 P3

CLEAN — checked and passing:
  Commit quality      17/17 conventional, every subject <=72 chars, every
                      feat/fix carries a body. No wip/fixup/asdf.
  Negative space      no schema change -> no migration owed; no new env var, no
                      new route handler, no feature flag, no new public API
                      endpoint, no new runtime dependency, no cron, no new
                      external fetch. Nothing owed.
  Rollback            no irreversible operations. No migration, no payment
                      mutation, no queue purge, no storage delete, no rm -rf.
                      The 15 replaced binary assets are tracked, so `git revert`
                      restores them.
  PR description      ## Summary and ## Test plan both present, body non-empty.
                      No issue link, but no commit references an issue number,
                      so none is owed.

RECOMMENDED NEXT STEPS:
  → No P1. Not blocked.
  → Decide the SCOPE question deliberately: split brand into its own PR, or
    accept the combined shape on the record.
  → Title trim is cosmetic.
