# Dead Code Scan — 2026-06-05

**Tool: knip (unconfigured)** | **Lang: ts** | **Project: D:\Bus-Booking** | **PR #7 @ 3fc5afba**

> ⚠️ knip ran WITHOUT a `knip.json`. On a Next.js app with 32 domain barrels + boundary-exempt
> `lib/core` + standalone `scripts/smoke/*` entry points, an unconfigured run reports many
> false positives (barrel entry files, script entry points, vitest setup). Findings below are
> hand-reclassified after a per-file importer grep. **First recommended action: author `knip.json`**
> (entry: `app/**/{page,route,layout}.tsx`, `scripts/smoke/*.{mts,mjs}`, `instrumentation.ts`,
> `vitest.*.setup.ts`; project: barrels) so future scans are signal, not noise.

## Summary

| Bucket | Count | Note |
|--------|------:|------|
| P1 high-confidence delete (after intent-confirm) | 3 | 0 importers, not entry/barrel/test |
| P2 review (exported / maybe-API / knip-unreachable) | 5 | confirm before cut |
| P3 deps + false-positives | ~16 | barrels/scripts (keep) + 2 dep findings |

## P1 — high-confidence delete (confirm "not pending-wire" first — these are RECENT rebuild additions)

| File / symbol | Type | Importers (grep) | knip | Verdict |
|---------------|------|-----------------:|------|---------|
| `components/op/KpiTile.tsx` | component | 0 | unused | DELETE — op-console redesign leftover; superseded by Overview 4-box? confirm. |
| `components/op/DetailLayout.tsx` | component | 0 | unused | DELETE — no consumer; confirm not pending-wire. |
| `lib/catalog/updatePickupPoint.ts` | service fn | 0 | unused | DELETE or WIRE — pickup-point edit service with no route/caller. Either an unwired feature (→ issue) or genuinely orphaned. |

## P2 — review needed

| File / symbol | Reason | Suggested action |
|---------------|--------|------------------|
| `components/op/DataTable.tsx` | knip says unused but grep finds 1 importer — the importer is itself likely unreachable | trace the one importer; if it's dead too, cut both |
| `components/op/FilterBar.tsx` | 1 importer (possibly dead chain) | same trace |
| `components/ui/radio-group.tsx` | shadcn primitive, 0 refs | keep if part of UI kit baseline; else cut |
| `lib/trips/schemas.ts` | may be barrel-exported zod schemas | confirm no `@/lib/trips` barrel re-export consumer |
| `lib/utils/useReducedMotion.ts` | 1 importer | likely live (motion polish); verify importer reachable |

## P3 — knip false positives (DO NOT DELETE — fix knip config instead)

- **Barrel entry files**: `lib/core/index.ts` + `lib/core/{config,errors,http,id,jobs,logger,money,result,time}/index.ts`, `lib/utils/index.ts`. These are the boundary-exempt `lib/core`/`lib/utils` public entry points (issue 092b). knip flags them only because it has no `project`/`entry` config. KEEP.
- **Entry-point scripts**: `scripts/smoke/{cross-persona-crawl,operator-crawl,traveler-crawl}.mts`, `scripts/smoke/hero-shot.mjs` — run via `tsx`/`node` directly, never imported. KEEP (add to knip `entry`).
- **`vitest.integration.setup.ts`** — referenced by vitest config, not an import. KEEP.
- `components/ui/sparkline.tsx` — knip flagged but grep finds 2 live importers. LIVE, false positive.

## P3 — dependencies

| Package | Type | Finding | Action |
|---------|------|---------|--------|
| `@sentry/nextjs` | unlisted | imported in `instrumentation.ts:15` but NOT in package.json `dependencies` | **P2-ish**: a clean `pnpm install` won't have it unless transitive. Either add to deps, or confirm the observability abstraction (issue 061) keeps it optional/lazy and the import is guarded. |
| `lint-staged` | unused devDep | config block exists in package.json | verify a husky/pre-commit hook actually invokes it; if no hook, remove the dep + config, else keep. |

## Safe-delete script

Generated at `scripts/safe-delete-2026-06-05.ps1` — contains ONLY the 3 P1 items, each behind a
manual-confirm prompt. **Review + confirm intent before running.** Does not auto-run.

## Next

- Author `knip.json` so the next scan is meaningful (this run's biggest finding is "knip is unconfigured").
- Confirm the 3 P1 items are not pending-wire features from the rebuild backlog before deleting —
  `updatePickupPoint` especially may want an issue to WIRE rather than a delete.
- Cross-ref docs/qa/debt-scan-pr7-20260605.md.

SUMMARY: 3 P1 (confirm-first) · 5 P2 · 2 dep findings · ~14 false-positives (need knip config)
