OBSERVABILITY REVIEW — PR #326 "feat(notifications): email customer confirmations + unmatched-payment notices"
──────────────────────────────────────────────────────────────────────────────────────────────────────────────
PR:        https://github.com/PhamAnhQuannn/bus-booking-vn/pull/326
Base/Head: fix/bank-transfer-reconcile-orphan ← feat/customer-email-notifications @ a55c3c6b
Decision:  (no review decision yet)
Size:      +280 / -29 across 10 files (diffed against `fix/bank-transfer-reconcile-orphan`, not `master`, per branch-stack instructions)
Generated: 2026-07-24T19:56:14Z

Project conventions detected:
  logger:  `logger.info/warn/error({ ...fields }, 'domain.event')` — pino instance from `lib/logger.ts`, imported directly (no barrel) in job/lib files.
  tracer:  none detected — no `@opentelemetry`/`tracer.startSpan` convention in this codebase; external-call observability is done via structured log + `captureException`.
  metrics: none detected (no `prom-client`/`statsd`/`counter.inc` convention) — `lib/observability/sentry.ts` `captureException`/`captureMessage` is the alerting substitute (Issue 061 AC5), already wired into both `processWebhook.ts`'s outer catch and `dispatchNotifications.ts`'s failure branch, unchanged by this PR.

Findings: 2  (P1: 0 · P2: 1 · P3: 1)

P1 — BLOCKING:
  None.

P2 — SHOULD FIX (follow-up, not a merge blocker):
  lib/jobs/reconcilePayments.ts:482  📡 P2: `opsUnmatchedPayment` — the sole ops-facing signal for a suspected/unmatched bank transfer — is enqueued with `channel: 'email'` hardcoded (no SMS/alternate-channel fallback, unlike every customer template in this same file which falls back to SMS via `booking.buyerEmail ? 'email' : 'sms'`). It rides through the identical NotificationLog → `dispatchNotifications` → `sendEmail` → Resend pipeline as the customer confirmation. If Resend is down or misconfigured (the exact failure mode `sendViaResend` already logs as `email.resend.api-error` / `email.resend.exception` in lib/notification/email.ts:167,173), BOTH the customer notice AND the ops alert fail identically — there is no independent channel for the "the interim substitute for an operator reconciliation screen" (the PR's own comment at reconcilePayments.ts:476). The failure is not silent (dispatchNotifications.ts logs `notify.dispatch.failed` and calls `captureException` on every attempt, including the 5th/exhausted one), but nothing distinguishes "the ops-alert channel itself is degraded" from routine per-message noise unless someone is actively watching Sentry/logs for `template=opsUnmatchedPayment`.
    Fix (follow-up, not this PR): either page on `opsUnmatchedPayment` rows that exhaust `MAX_ATTEMPTS` specifically (a distinct `captureMessage`/alert fingerprint), or give the ops alert a second channel (e.g., SMS to an ops number) independent of the Resend dependency.

P3 — ADVISORY:
  lib/jobs/reconcilePayments.ts:409,435,480,495,589 and lib/payment/processWebhook.ts:398-414  📊 P3: None of the `enqueuePendingNotification` / `createNotificationLog` call sites (existing 2 + this PR's 3 new templates) log on successful enqueue — only failure is logged (P2002 swallowed with `reconcile.enqueue_skipped` in reconcilePayments.ts:637, or bubbled to the outer `catch` + `captureException` in processWebhook.ts:447-460). This matches the pre-existing pattern for `customerBookingPaid`/`operatorNewBooking` so it is not a regression introduced by this PR, but it means "was a customerPaymentReview/opsUnmatchedPayment notice actually queued for booking X" is only answerable by querying `NotificationLog` directly, not from the log stream. Low cost since the row write itself is the durable record.
    Consider (optional): a single `logger.info({ bookingId, template, channel }, 'notify.enqueued')` inside `enqueuePendingNotification`'s try block, mirroring the existing failure-path log shape.

CONFIRMED CLEAN (per task's specific checks):
  - Cat 1 (console.* instead of structured logger): zero `console.*` added anywhere in the diff — verified via `git diff fix/bank-transfer-reconcile-orphan...HEAD | grep '^+' | grep -i console`.
  - Cat 3 (swallowed catch): `sendViaResend` (lib/notification/email.ts:151-176, unchanged by this PR but newly exercised by the 4 new email templates) logs `email.resend.api-error` on a Resend API error and `email.resend.exception` on a thrown exception, returning `ok:false` in both cases — never silent. `dispatchNotifications.ts` (unchanged, pre-existing) turns that `ok:false` into `status='failed'` + exponential backoff + `logger.warn('notify.dispatch.failed')` + `captureException(..., { area:'notification', notificationId, channel })` on every failed attempt up to `MAX_ATTEMPTS=5`. So a real email delivery failure is logged, retried, and alerted — not silent.
  - PII in new/changed log lines: audited every `logger.*` call touched or added in this diff (reconcilePayments.ts:368,376,443,506,522,595,637; email.ts:167,170,173,193; processWebhook.ts unchanged call sites). All new fields are `bookingRef` / `paymentEventId` / `providerTxnId` / `amountVnd` / `heldForHours` / `template` / `externalRef` / `subjectLen` / `bodyLen` / `recipientLen`. No `buyerEmail`, no payer/buyer name, no `rawBody`, no raw `recipient` string appear in any log line — `email.ts` explicitly logs `recipientLen: to.length` instead of the address, and `dispatchNotifications.ts:158` has an inline comment noting `recipient` is deliberately excluded as PII. Clean.
  - The three new templates (`customerPaymentReview`, `customerPaymentUnverified`, `opsUnmatchedPayment`) and the email/SMS channel switch all funnel through the SAME pre-existing dispatch/logging/alerting infrastructure as the templates that shipped before this PR (`customerBookingPaid`, `customerBookingExpired`) — no new code path bypasses it.

RECOMMENDED NEXT:
  - No P1s — does not block merge.
  - File the P2 (ops-alert SPOF) as an explicit follow-up issue rather than blocking #326; it's a design/reliability gap in an already-documented "interim" mechanism (reconcilePayments.ts:476 comment self-identifies it as interim), not an instrumentation absence — the failure path IS logged and alerted today.
  - Optional: fold the P3 enqueue-success log into a later notification-domain cleanup pass; not worth a dedicated PR.

SUMMARY: 0 P1 · 1 P2 · 1 P3 · pinned to a55c3c6b
