# PR INBOX — consolidated review sweep

**Generated:** 2026-07-28
**Scope:** all 6 open PRs on `PhamAnhQuannn/bus-booking-vn`
**Agents:** 4 (code-review · pr-review · supply-chain/backcompat · security-deep), read-only, parallel

---

## Ranked verdicts

| # | PR | Shape | Verdict | Blocking finding |
|---|----|-------|---------|------------------|
| 1 | **#398** docs(claude): log review round; fix pickup comment drift | comment-only, 3 files, +22/-4 | **HOLD — needs change** | comment + PR body assert a completeness the sweep did not achieve; #366 already closed on that claim |
| 2 | **#387** shadcn 4.12.0 → 4.15.0 | dep bump | **MERGE** (rerun CI) | none — CI failure is a docker-registry flake, 0 tests executed |
| 3 | **#386** lucide-react 1.22.0 → 1.27.0 | dep bump | **MERGE** | none — cleanest of the five, zero findings |
| 4 | **#385** @playwright/test 1.61.1 → 1.62.0 | dep bump (dev) | **MERGE** | none |
| 5 | **#384** @sentry/nextjs 10.65.0 → 10.68.0 | dep bump | **MERGE** | none — no PII delta at current config |
| 6 | **#383** @tailwindcss/postcss 4.3.0 → 4.3.3 | dep bump (dev, lock-only) | **MERGE after visual pass** | only PR of the five that changes rendered output |

**Totals across all four reports:** 2 P1 · 12 P2 · 20 P3

---

## P1 — outside the PR set, dominates it

### `next@16.2.10` carries an unpatched App Router middleware/proxy bypass

`pnpm audit --prod` against master's lockfile: **30 vulnerabilities — 10 HIGH, 20 moderate, 0 critical.** CI's gate is `--audit-level=critical` (`.github/workflows/ci.yml:339`, downgraded from `high` per the WT-21 mistake-log entry), so this exits 0 today.

The worst is `next@16.2.10` (`package.json:48`, pinned exact, no caret): 4 HIGH + 5 MODERATE, **all fixed in 16.2.11**, including **GHSA-6gpp-xcg3-4w24 — Middleware / Proxy bypass in App Router.**

This repo routes auth guards, CSRF, and rate-limiting all through `proxy.ts`. One bypass, three controls.

None of the six open PRs touches it. **This is the highest-value dependency action available and it is not on the board.** File it.

Related, same class, one-line each:
- `pnpm.overrides.postcss: ">=8.5.10"` (`package.json:102`) — floor set 8 patches below the HIGH line (`<=8.5.17`). Three of the five PRs bump *a* postcss instance and leave `.>next>postcss@8.5.16` vulnerable. `>=8.5.18` fixes it.
- Same defect on the sibling `@hono/node-server: ">=1.19.13"` override.

---

## P1 — #398, converged on independently by two agents

Both #398 agents flagged the same defect from unrelated lenses (line-level claim verification vs. diff-shape negative space). Per this project's own 2026-07-28 convergence rule, that makes it structural rather than taste.

**The claim:** `lib/booking/getManifest.ts:9` and the PR body assert `pickupAreaLabel` "no longer exists anywhere."

**It does.** Cross-checked directly (`grep -rn "pickupAreaLabel\|createOperatorPickupArea\|OperatorPickupArea" lib app components prisma e2e`, migrations excluded):

- **`lib/geo/data/PROVENANCE.md:27`** — the one live, present-tense survivor, and the worst. Not a stale name: it asserts a *safety property* — *"Booking rows snapshot the resolved label (`pickupAreaLabel`) so a future swap does not orphan historical pickups"* — that migration `20260622100000:83,85` voided by dropping both columns. It is the stated justification for the dataset re-versioning procedure, sitting in the same directory as `lib/geo/vnAdmin.ts`, which this PR *did* fix.
- `lib/api/routesClient.ts:12`, `lib/catalog/getRouteById.ts:3`, `lib/core/validation/route.ts:3`, `e2e/op-routes.spec.ts:215` — past-tense historical notes naming `OperatorPickupArea` as the replacement. Weaker, but they falsify the literal quantifier.

Two more the symbol grep cannot reach, found by the shape agent because the question is concept-shaped, not symbol-shaped:

- **`components/geo/AdminUnitPicker.tsx:7-8`** — a "Used by:" caller list naming the deleted console pickup-area menu and customer pickup step. Byte-for-byte the defect class this PR fixed in `vnAdmin.ts` — and `vnAdmin.ts` cross-references this exact file three lines above its own fix.
- **`prisma/schema.prisma:62`** — "Bounds the default pickup-area picker," in the very file the PR body cites as evidence of cleanliness.

**Root cause:** a two-symbol grep answering "did any dangling reference survive." The PR's own diff states the governing rule — *"'source X does not have field Y' is only a valid claim for sources whose fields you have actually enumerated."*

**Why P1 and not P2:** issue **#366 is already closed** on the assertion that the two doc comments were "the only thing this sweep turned up" (closing comment posted 97s after the commit). The survivors have no owner. Squash-merge makes the false claim permanent master history — the exact failure mode two of the twelve new log entries in this same diff exist to prevent.

**Minimum to unblock (~5 min, no code change):**
1. Narrow the quantifier in `getManifest.ts:9` to what was actually verified — "in `schema.prisma` or any TypeScript source."
2. Delete the stale clause at `lib/geo/data/PROVENANCE.md:27`.
3. Narrow the `## #366` paragraph in the PR body to the two-symbol grep performed.
4. Reopen #366, or file a successor, for the remainder.

---

## P2 highlights

**#398**
- `CLAUDE.md` grows **42,916 → 59,930 bytes (+40%) in one PR** — 35 log entries on the file injected into every agent session, ≈ +4,300 tokens per invocation, forever, no rotation policy. Invisible to line-level and architecture review by construction.
- `docs/qa/code-review-pr125-20260623.md:112` already recorded the `getManifest` finding a month ago; `architect-review-pr125-20260623.md:121/137` enumerated a **superset** including the survivors this PR missed. Neither cited nor declined, so "verified, not assumed" does not hold.
- Three new log entries credit **#382**, which is closed and never merged. The carrier is **#397** (`e66d19d`). The log narrates the closure but never names the replacement — future sessions chasing #382 hit a dead end.
- `lib/core/db/holdRepo.ts:5,10` still name blocking `pg_advisory_xact_lock` while `:137,:160` use try-locks, and `:19` contradicts both. Same drift class, in the file these new entries are *about*. Out of diff.

**#383 — the sleeper.** Smallest, most ignorable PR of the five (lock-only, devDep, patch) and the **only one that changes what a customer sees.** The CSS engine compiling `app/globals.css` is the copy under `@tailwindcss/postcss` → `@tailwindcss/node`, which was **4.3.0** — not the top-level `tailwindcss@4.3.3` devDep. Crosses 4.3.1's `Changed` section: spacing utilities emit `0`/`var(--spacing)` instead of `calc(...)`, Preflight font-stack swap away from `system-ui` for CJK, case-insensitive hex→theme-color collapsing, `oklch` achromatic hue fix. Bounded by `app/globals.css:11` pinning `--font-sans: var(--font-be-vietnam)`. Also the only PR that *shrinks* the tree — genuine dedup of duplicate `tailwindcss`, `enhanced-resolve`, `nanoid`, `postcss`.

**#384 — Sentry.** No PII delta at current config, verified against installed SDK source: `sendDefaultPii` and `dataCollection` both unset → `userInfo:false`, cookies/headers/query-params deny-listed, `httpBodies:[]`, and 10.66's two new keys land safe. `tracesSampleRate:0` neutralises 10.68's new `URL.full`/`URL.path` span attributes. Replay not in default integrations.

Forward-looking trap worth pinning: `const base = options.dataCollection != null ? DEFAULTS : ...` — setting `dataCollection` to **any** object, even `{databaseQueryData:false}`, flips the base fully permissive (cookies, request *and* response headers, all four HTTP body categories, query params, userInfo). 10.66 adds exactly the two sub-keys that invite a maintainer to reach for it. The repo's `beforeSend` scrubs only `event.extra`/`event.contexts`, so it would not catch it — and `event.request.url` is captured unconditionally, which matters because `/booking/result/[token]` and `/booking/confirmation/[token]` carry ticket tokens in the path.

Also: 10.66's `dataCollection.databaseQueryData`/`graphQL` attach data to **span attributes**, outside the same scrubbers. No exposure at sample rate 0 — but enabling tracing must extend the scrubber in the same commit.

**#387 — `shadcn` in prod `dependencies`** (`package.json:54`). Nothing imports it at runtime: exhaustive grep returns two hits, neither a JS/TS import (`components.json:2` `$schema`, and the manifest line itself).

Measured blast radius of moving it to `devDependencies`, by walking the lock's `snapshots:` graph both ways:
- Production closure **769 → 537 packages (−232, −30%)**
- Dropped: `express@5.2.1`, `express-rate-limit`, `hono@4.12.27`, `cors`, `body-parser`, `serve-static`, `@modelcontextprotocol/sdk`, `@dotenvx/dotenvx`, `systeminformation` (sole consumer in the whole tree), `undici`, a second `zod` major (3.25.76 alongside the app's v4), the full `@babel/*` + `ts-morph` codemod stack
- On-disk install shrinks by only 186 — 46 of the 232 already sit in the dev closure. **The win is in what `pnpm audit --prod` walks.** Per WT-21 that gate was already downgraded from `high` because of `prisma → @prisma/dev → hono`; shadcn supplies a *second independent path to hono* plus a full Express stack
- Runtime behavior and served bundle size: **zero change**

Caveat the "it's just a CLI tool" framing misses: `app/globals.css:3` does `@import "shadcn/tailwind.css"`, so it is a **build-time** dependency of the CSS pipeline. Safe on Vercel and in CI (both install devDeps), breaks only on a hypothetical `pnpm install --prod && next build` path, which does not exist today. That caveat belongs in the PR body — squash-merge makes it the permanent commit message.

---

## What the dep-bump batch is *not*

Both dep agents independently established the same structural fact, which forecloses most of the supply-chain checklist:

**Zero new package names enter the lockfile in any of the five PRs.** Verified mechanically — every `+`-side package key reduced to its bare name and set-differenced against master's complete `packages:` name set. 19/6/11/57/20 names touched; **none new.** No license drift, no typosquat surface, no new-maintainer exposure.

Lifecycle scripts are a non-issue twice over: `grep requiresBuild pnpm-lock.yaml` → 0 hits, and `pnpm-workspace.yaml:5` `onlyBuiltDependencies` allowlists only `@prisma/engines` + `prisma`, so pnpm 10 blocks everything else by default.

No advisory exists against any of the five target versions or any transitive they move.

---

## Merge sequencing

All five dep PRs mutate the same `browserslist` / `electron-to-chromium` / `postcss` lock lines, so **"all five are green" only holds one at a time.** The first merge forces a Dependabot rebase and a genuine CI re-run on the other four — do not read the current green as covering the post-merge state.

Recommended order: **#386 → #385 → #383 → #384 → #387.**

One subtlety: **#387 is the only PR of the five that closes an advisory** — it bumps `fast-uri` 3.1.2→3.1.4, closing 2 HIGH. Both sit on paths rooted at `@sentry/nextjs`, and **#384, the actual Sentry bump, closes neither.** Which incidental transitives get healed depends on which Dependabot PR regenerates the lock last. Audit the *merged* result, not each PR.

#387 also needs its Integration Tests re-run before merge — the failure is `docker pull postgres:16` → `Get "https://registry-1.docker.io/v2/": context deadline exceeded`, three retries, zero tests executed. E2E chromium, mobile-390, and the Vercel build all passed on the same commit.

---

## RECOMMENDED NEXT

1. **File the `next@16.2.10` → `16.2.11` bump.** Highest-value dependency action available, on the middleware that carries auth + CSRF + rate-limit, and absent from the current PR set.
2. **Unblock #398** with the 4-step body/comment narrowing above, and reopen #366.
3. **Merge the dep bumps** in the order above, re-running #387's flaked job first.
4. **Follow-ups, own issues:** move `shadcn` to `devDependencies` (separate PR, not folded into #387) · raise the `postcss` and `@hono/node-server` override floors · pin the Sentry `dataCollection` trap in a comment at the init sites · CLAUDE.md rotation policy · `@playwright/mcp@0.0.78` pins `playwright-core@1.62.0-alpha-...` (`pnpm-lock.yaml:4569`), pre-existing and untracked.

**No PR was commented on, merged, or modified. No source file outside `docs/qa/` was touched.**

## Artifacts

- `docs/qa/code-review-pr398-20260728.md`
- `docs/qa/pr-review-pr398-20260728.md`
- `docs/qa/backcompat-deps-20260728.md`
- `docs/qa/security-deep-deps-20260728.md`
