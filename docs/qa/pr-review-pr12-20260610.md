PR REVIEW — PR #12 "feat(pickup): route-scoped pickup areas + trip display polish (issue 113)" @ 8c15605a
─────────────────────────────
Diff scope: 263 files, +8962 / -14752 lines, 28 commits
State: open (ready)

PRIORITY 1 — Block push, fix first:
  [SCOPE] 10+ distinct intents across 28 commits.
    Detected: feat(sms/eSMS adapter), feat(db/operator-fields), feat(auth/username-login),
    feat(onboarding/admin-provisioning), feat(geo/VN-admin-dataset), feat(pickup/issues 103-113),
    feat(ui/SEO+home), refactor(trips), chore(infra+scripts), docs(cleanup+go-live)
    Context: branch accumulated incrementally as pickup-areas feature + prerequisites were
    built. Core theme (pickup issues 103-113) is coherent; eSMS/SEO/admin-provisioning are
    unrelated passengers.
    Impact: cannot bisect, revert, or review any single concern in isolation.
    Fix: consider squash-merge and accept the bundled scope (pragmatic for solo-dev pre-launch),
    OR split unrelated passengers into separate PRs via /commit-split.

  [SIZE] 8962 additions + 14752 deletions across 263 files.
    After excluding generated files (8 migrations, docs, screenshots): still ~6K+ net lines
    across ~180 non-generated files. Far above 800/40 threshold.
    Context: branch diverged over ~5 days of incremental feature work (issues 103-113).
    Fix: same as SCOPE — squash-merge or split.

PRIORITY 2 — Fix before merge:
  [PR DESC / SCOPE MISMATCH] PR title + body describe only Issue 113 (latest commit).
    Actual PR ships issues 103-113 + eSMS adapter + admin provisioning + SEO routes +
    geo dataset + auth username login + docs cleanup.
    Fix: update PR body to summarize ALL shipped work, or retitle to reflect full scope
    (e.g. "feat(pickup): pickup areas v1+v2 end-to-end (issues 103-113) + infra").

PRIORITY 3 — Address when convenient:
  [PR DESC / TITLE] Title is 71 chars (limit 70). Cosmetic.
  [COMMIT MSG] Most commits follow conventional format. No wip/fixup/asdf subjects. Clean.

NOTES:
  - Negative-space: new env vars in .env.example ✓. Schema changes have migrations ✓.
    New route handlers (pickup-areas CRUD, geo, trip pickup-areas) have tests for core
    logic (setRoutePickupAreas, operatorPickupArea, pickupSelection, vnAdmin, trustDisplay,
    esmsClient). Some route-level tests deleted (legacy pickup-points) and replaced by new
    tests for the replacement model. Coverage is adequate for pre-launch.
  - Rollback: no irreversible ops (no DROP COLUMN/TABLE). All migrations are additive
    (CREATE TABLE, ADD COLUMN, backfill). Safe to revert by rolling back migrations.
  - Commit messages: well-structured conventional commits with bodies on feat/fix commits.
    Each issue gets its own commit with clear scope description.
  - No new runtime deps added (no package.json dep changes beyond devDeps).

SUMMARY: 2 P1, 1 P2, 2 P3

RECOMMENDED NEXT STEPS:
  → SCOPE/SIZE P1: for a solo-dev pre-launch project, pragmatic path is squash-merge with
    an updated PR description. The alternative (splitting 28 commits into separate PRs
    retroactively) is high-effort / low-value at this stage.
  → Update PR body to cover full branch scope (P2 fix).
  → Decision point: override SCOPE/SIZE P1 as accepted-risk for pre-launch, or split.
