PERF REVIEW — PR #408 "refactor(tourism-kb): relocate tourism KB into a standalone feature"
─────────────────────────────────
PR:        https://github.com/PhamAnhQuannn/bus-booking-vn/pull/408
Base/Head: master ← feat/tourism-kb-relocation @ b9e7a5bf
Size:      +437 / -98,292 across 842 files
Generated: 2026-08-02

No perf findings.
(No N+1, no unindexed hot paths, no heavy imports, no new always-on cost.)

Scan detail:
  - prisma/schema.prisma NOT touched → no index/query-shape concerns.
  - package.json NOT touched → no new runtime dep, no bundle weight added.
  - No prisma.*/findMany/findUnique/count/setInterval/cron in added CODE lines. The only runtime
    change (fix(status)) swaps three local status-map lookups for one `bookingStatusDisplay()` call —
    a pure in-memory Record lookup, no I/O.
  - Client-bundle delta is NEGATIVE: the two account pages replace a local `./bookingStatus` import
    with `@/lib/op/statusLabels` (already in the op bundle) and the confirmation page DELETES two
    inline map objects. No moment/lodash/heavy import introduced.
  - The 51 relocated tourism scripts are offline Python/CLI (run manually or in the CI guard job), not
    request-path code — no runtime cost surface.

SUMMARY: 0 P1 · 0 P2 · 0 P3 · pinned to b9e7a5bf
