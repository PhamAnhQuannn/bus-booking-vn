# PR REVIEW — PR #403 "fix(hero): sharpen the bus, real BBVN livery, DPR-tiered delivery" @ `3915282c`

**Base:** `master` · **Head:** `feat/hero-sharpen` · **State:** open, ready, `MERGEABLE`/`CLEAN`
**Diff scope:** 19 files (9 text + 10 binary), text net +1095, 2 commits · **CI:** 13/13 · **Date:** 2026-08-01

Shape-level pass. Line-level findings in `docs/qa/code-review-pr403-20260801.md`.

---

## PRIORITY 1

None.

---

## PRIORITY 2

### [SIZE] Text net +1095 lines is over the 800 threshold — waiving, with the reason stated

19 files is under the 20-file bar; the line count is not. Composition:

| | files | net |
|---|---:|---:|
| new standalone Python tools (`hero-cut/grade/logo/verify.py`) | 4 | ~700 |
| docs (`CREDITS.md`, `hero-master/README.md`) | 2 | ~150 |
| smoke scripts | 2 | ~180 |
| **app source** | **1** | **~65** |
| binary assets | 10 | — |

**Recommend accepting rather than splitting.** Only one file (`app/(customer)/page.tsx`) is
application code, and the ~65 lines there are the actual behaviour change. The rest is new
build-time tooling that has no consumer until it lands, plus the documentation of what it produces
— splitting it would create a PR of tools nobody calls followed by a PR that calls them, which is
the same objection the tourism PR raised about separating a generator from its consumer.

Record the reason in the body before squash-merge so it reads as a decision.

---

## PRIORITY 3

### [PR DESC] No `## Summary` / `## Test plan` headings
Sections are `## Why the bus was blurry`, `## What changed`, etc. Substantively both are present and
the narrative headings are arguably clearer here. Third PR in a row with this shape — it is a house
convention question, not a per-PR defect.

---

## Verified clean

- **Scope is one thing.** Both commits are `hero`-scoped (`feat` then `fix`), and the second is a
  genuine follow-up defect (sub-pixel decal), not unrelated work smuggled in.
- **Commit messages carry 4,540 and 4,870-character bodies** explaining why. Zero `wip`/`fixup`.
- **Negative space — nothing missing.** No schema, migration, env var, route handler, feature flag,
  cron, dependency or external API call anywhere in the diff, so every companion-artifact trigger is
  inapplicable rather than unmet.
- **No irreversible operation.** Rollback is `git revert`; the only durable cost is repository size,
  which is called out in the code review.
- **The body is evidence, not argument** — and unusually so. It reports a **negative** result about
  the author's own prior work: the "2× AI upscale" recorded in `b489680` was never super-resolution,
  proven by mean abs difference **0.61/255** against a plain Lanczos resize with *identical*
  Laplacian variance (624.17 both). A body that disproves an earlier claim of its own author is the
  opposite of the failure mode `CLAUDE.md` 2026-07-30 logs.
- **Improvement is quantified per breakpoint**, with the invariant checked alongside it: bus
  sharpness 2.30×–5.12× up, and low-frequency SSIM **0.9965–0.9994** confirming the framing did not
  move. Measuring both the thing you wanted to change and the thing you did not is the right shape.
- **A third contributing cause is named honestly** — a 40–55% white wash sitting over the vehicle on
  mobile, which the body says mattered more than resolution on a phone. Easy to omit; it reframes
  the whole finding.

## Not reviewed

- Whether the visual result is good. Design judgement, and per `CLAUDE.md` 2026-08-01 a `:3001`
  measurement after a branch switch describes Turbopack's on-disk cache rather than the diff.

---

```
SUMMARY: 0 P1, 1 P2 (waived), 1 P3
```

## RECOMMENDED NEXT STEPS

1. **Merge with `gh pr merge 403 --squash` and NO `--delete-branch`.** PR #405 is based on
   `feat/hero-sharpen`; deleting it auto-closes #405 and `gh pr reopen` refuses once the base is
   gone (`CLAUDE.md`, 2026-07-28). Delete the branch only after #405 has been re-targeted.
2. Add the size waiver reason to the body before merging — it becomes the permanent commit message.
