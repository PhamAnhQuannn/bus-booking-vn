PR REVIEW — PR #301 "feat(ledger): migrate Payout to BigInt + Neon index readiness" @ b4cd55f6
─────────────────────────────
Mode: PR (re-review — supersedes docs/qa/pr-review-pr301-20260716.md @ f66a1caf, which
      returned 0 P1 / 0 P2 / 0 P3 against a 1-commit, 23-file version of this PR)
Diff scope: 33 files, +677 / -79, 4 commits
Base: master · Head: feat/payout-bigint-neon
State: OPEN, ready (not draft) · mergeable: MERGEABLE · mergeStateStatus: CLEAN
Opened 2026-07-16 · last pushed 2026-07-25 · 10 days open
URL: https://github.com/PhamAnhQuannn/bus-booking-vn/pull/301

STATUS: HELD by policy — blocked on #362 + #363. mergeStateStatus CLEAN is misleading:
GitHub sees no conflict and green checks, so nothing mechanical prevents a merge.
The hold is entirely human-enforced. That is itself a risk on a PR this old.


PRIORITY 1 — Block push, fix first:

  [PR DESCRIPTION / MISTAKE-LOG MATCH] PR body vs final diff — body is stale on 5 counts
    CLAUDE.md 2026-07-24 makes this an auto-P1: "before squash-merging a multi-commit PR,
    re-read the PR body and any safety/rollback doc against the FINAL diff — they were
    written against the first commit." This body was written at commit 940438e and never
    updated across d0f1381, 8618573, b4cd55f. On squash-merge it becomes the permanent
    master commit message. Concretely wrong or missing:

    1. WRONG — "Migration: `20260715010000_neon_readiness`". No such directory exists.
       Commit b4cd55f re-timestamped it to `20260725120000_neon_readiness`. The permanent
       commit message would point a future bisect at a migration that was never committed.

    2. MISSING — the `DATABASE_POOL_MAX` default flip 5 → 1 and
       `connectionTimeoutMillis` 3s → 10s in lib/core/db/client.ts. This is the single
       most operationally consequential change in the PR — it is the reason the PR is
       held, the reason #362 and #363 exist, and it is not mentioned in the body at all.
       A reader of master's history would have no idea prod pool sizing changed here.

    3. MISSING — `@@index([operatorId, createdAt])` on LedgerEntry. The body enumerates
       three index changes (Trip.busId, Hold, Operator) and omits the fourth, which the
       migration itself labels "P1-2".

    4. MISSING — `.env.example` and three `.github/workflows/ci.yml` job env blocks gain
       `DATABASE_POOL_MAX=5`. These exist only to keep dev/CI/e2e off the new prod default;
       without them in the body, a future reader who deletes them will not know why.

    5. MISSING — the response shape of POST /api/op/reports/payouts/[id]/retry changed
       (6 money fields JSON number → JSON string). A wire-contract change, undocumented.

    Fix: rewrite the body against `gh pr diff 301` before any merge. Treat it as the
    commit message it will become.

  [ROLLBACK PATH] prisma/migrations/20260725120000_neon_readiness/migration.sql
    The migration contains three irreversible-or-lossy operations and the PR body has NO
    rollback section, and no `docs/migrations/20260725120000_neon_readiness-safety.md`
    exists — even though that filename convention is established, with BOTH migrations
    currently on master carrying one.
      · `ALTER COLUMN ... SET DATA TYPE BIGINT` ×6 on Payout — widening, so reversing to
        INTEGER FAILS outright once any stored value exceeds 2^31−1. Which is the entire
        stated purpose of the change.
      · `DROP INDEX IF EXISTS "Hold_expiresAt_idx"` — reversible only by a rebuild scan
        on the hottest write table.
      · `DROP INDEX IF EXISTS "Operator_id_idx"` — reversible, cheap.

    The CLAUDE.md 2026-07-24 rule also requires stating rollback in terms of what the
    OTHER-version code does against the migrated schema, not just the schema shape. The
    2026-07-16 pr-review got this exactly backwards — it wrote "Int→BigInt is
    backward-compatible ... Standard rollback: forward migration to revert" and closed
    the category OK. That is wrong in the dangerous direction: reverting the CODE
    (schema.prisma back to `Int`) while leaving the columns BIGINT means Prisma reads an
    int8 column through an `Int` field via @prisma/adapter-pg. That is a silent money
    deserialization hazard on the payout path, not a no-op.
    Fix: write the safety doc; state code-revert-only, migration-revert-only, and
    both-reverted separately. See the /migration-safety report.

  [SCOPE / ARTIFACT POLLUTION] docs/qa/code-review-pr300-20260716.md,
                               docs/qa/pr-review-pr300-20260716.md
    This PR ships two review reports for a DIFFERENT pull request (#300). They entered
    via commit d0f1381 "docs(qa): add review reports for PR #300 and PR #301". Merging
    #301 would land #300's review artifacts on master regardless of #300's own fate.
    Fix: drop the two pr300 files from this branch.


PRIORITY 2 — Fix before merge:

  [SCOPE DISCIPLINE] Three intents, one PR
    a) feat(ledger)  — Payout Int→BigInt + every consumer + DTO string boundary
    b) perf(db)      — four index changes for Neon readiness
    c) config(infra) — pool max 5→1, connect timeout 3s→10s, .env.example, 3 CI jobs
    (a) and (b) are legitimately coupled: same models, same deploy, same migration. (c)
    is not — it shares only the word "Neon". It is the piece that is blocked on #362/#363,
    it has an entirely different risk profile and rollback (a config revert, not a
    migration), and it is the reason the whole PR has sat for 10 days.
    The 2026-07-16 pr-review called scope "OK (borderline)" against a 2-intent diff.
    Commit b4cd55f added the third.
    Fix: carve (c) into its own PR stacked behind #363. (a)+(b) then have no blocker and
    could ship on their own merits.

  [COMMIT MSG] b4cd55f "fix(ci): give test env a >1 db pool + re-timestamp migration
    after rebase"
    Conventional form and length are fine, but the commit bundles a CI env change with a
    migration rename, and the `fix(ci):` type hides the migration rename from anyone
    scanning the log for schema-affecting commits. On a squash-merge this disappears
    entirely — which is precisely why the body must carry it (see P1 #1).

  [PR DESC] Test plan is four unchecked boxes
    All four are in fact satisfied — CI on b4cd55f is green across Lint & Typecheck, Unit,
    Integration, E2E (chromium + mobile-390), flaky-e2e, data-leak, dep-audit,
    greppable-invariants, gitleaks, and the Vercel preview deploy. An all-unchecked test
    plan on a 10-day-old ready PR reads as "never validated" and invites a reviewer to
    re-do work already done.
    Fix: check the boxes, or replace with a link to the green run.


PRIORITY 3 — Address when convenient:

  [PR DESC] No issue linkage. The migration SQL comments reference hardening IDs
    (`P0-2`, `P1-1`, `P1-2`, `P1-3`, `P1-6`) with no pointer to the audit they came from,
    and the body does not reference #362, #363, or #364 despite this PR being gated on
    the first two and touching the file the third is filed against.
    Fix: add `Blocked by #362, #363` and `Refs #364` to the body.

  [DIFF SIZE] OK. Headline +677 is misleading: 544 of those additions are the 8
    docs/qa markdown reports and 20 are the generated migration. Reviewable source+test
    net is roughly +113/−79 across 25 files — comfortably inside thresholds.

  [STALENESS] 10 days open, 3 commits after the reviews it carries. Every one of the five
    2026-07-16 reports in this branch describes "23 files, +100/-78, 1 commit @ f66a1caf".
    They are now stale by construction and will land on master describing a diff that no
    longer exists. See STALE-REVIEW DELTA in the summary report.
    Fix: refresh or delete them before merge; do not ship a review doc that describes a
    superseded diff.


NEGATIVE-SPACE AUDIT

  [OK]   Schema change → matching migration present.
  [OK]   Env var → `.env.example` updated with `DATABASE_POOL_MAX="5"` plus a comment
         explaining the prod-vs-dev split. `DATABASE_POOL_MAX` also already carried a
         Zod-validated definition in lib/config/env.ts:303 (default 1).
  [OK]   No new route handler, no new server action, no new public endpoint.
  [OK]   No new runtime dependency → no license note needed.
  [OK]   No new feature flag, no new cron, no new external API call.
  [FAIL] Irreversible migration op → no rollback plan. (P1 above.)
  [FAIL] Migration added → no `docs/migrations/<name>-safety.md`, against repo convention.
         (P1 above; detailed in the /migration-safety report.)


SUMMARY: 3 P1, 3 P2, 3 P3

RECOMMENDED NEXT STEPS:
  → Do not merge. Held on #362 + #363.
  → Highest-value single action: rewrite the PR body against the final diff. Everything
    else is recoverable post-merge; a wrong squash-merge commit message is permanent.
  → Second: carve the client.ts/env/CI config change into its own stacked PR. That
    unblocks the ledger + index work independently of #362/#363.
  → Third: drop the two pr300 docs and refresh or delete the five stale pr301 docs.
