PR REVIEW — PR #408 "refactor(tourism-kb): relocate tourism KB into a standalone feature" @ b9e7a5bf
─────────────────────────────
Diff scope: 842 files, +437 / −98,292 lines, 10 commits
PR exists: yes
State: open (ready, not draft)

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  [PR DESC / STALE BODY] PR body materially contradicts the final diff.
    The body was written for commit 1 (c7922ba, the relocation) and never walked forward over the
    9 commits pushed on top. Two concrete false claims:
      1. "This PR is tourism-only." — FALSE. The PR now also carries a runtime behavior fix
         (`fix(status): unify three drifted booking-status label maps`, touching lib/op +
         app/(customer) + components), dead-code removal, docs archiving, and CLAUDE.md edits.
      2. "Notes for the reviewer — Not in this PR: 8 pre-existing unstaged deletions under
         components/+lib/utils/ ... deliberately kept out of every commit here." — FALSE. Commit
         038b497 committed EXACTLY those 8 files (lib/utils/index.ts, lib/utils/useReducedMotion.ts,
         components/home/{FeatureHighlights,IntroBanner,RouteDirectory}.tsx,
         components/op/{DetailLayout,KpiTile}.tsx, components/ui/radio-group.tsx).
    Risk: this is a squash-merge PR, so the body becomes the permanent master commit message. Shipping
    it would write a commit that claims tourism-only scope and promises exclusions the diff violates —
    the exact CLAUDE.md mistake-log pattern (2026-07-24 stale-body-into-history; 2026-07-30 false
    PR-body claims).
    Fix: rewrite the PR body to describe all 10 commits (relocation + status-unify + cleanup + docs)
    and delete the now-false "tourism-only" / "not in this PR" notes BEFORE squash-merging.

  [SCOPE] 10 commits spanning 4 intent types (refactor / fix / chore / docs) in one PR.
    refactor(tourism-kb) and fix(status) (booking-status map unify) share no thread — the latter is an
    independent runtime change. Strict rubric = P1-split. Downgraded to P2 because: every commit is
    low-risk repo-hygiene on a deliberate cleanup branch, none needs independent CI/review, and the
    842-file scope was reviewed at ship-plan time and accepted as a single PR (user chose stop-at-green,
    manual merge). Informational — the merge decision is the user's.

PRIORITY 3 — Address when convenient:
  [COMMIT MSG] b9e7a5b subject is malformed: "@ chore(cleanup): remove post-refactor dead code + fix
    stale doc refs" — leading "@ " breaks conventional-commit form (subject must start with a type).
    Cosmetic here since squash-merge lets the user author the final message, but fix if amending.

  [SIZE] 842 files / −98,292 net is far over the >40-file / >800-line P1 threshold, but is NOT a review
    burden: ~790 files are pure renames (scripts/tourism→tourism-kb/code, docs/qa→docs/archive) and
    untrack-deletions (.claude/skills 490 files, tourism data). Reviewable surface is 18 M-files + 24
    edited-renames. Treated as the lock-file/generated exception — noted, not blocking.

SUMMARY: 0 P1, 2 P2, 2 P3

RECOMMENDED NEXT STEPS:
  → Before you squash-merge #408: UPDATE THE PR BODY. It currently describes 1 of 10 commits and makes
    two false scope claims that would ship verbatim into the master commit message.
  → Scope + size findings are informational (bundle already accepted at plan time).
  → No hard blockers to CI.
