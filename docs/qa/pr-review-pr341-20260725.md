PR REVIEW — PR #341 "fix(payments): validate SePay account + rate-limit bank_transfer webhook (#334, #329)" @ 19633e16
─────────────────────────────
Diff scope: 7 files, +129 / -11 lines, 1 commit
PR exists: yes
State: open (ready)

PRIORITY 1 — Block push, fix first:
  (none)

PRIORITY 2 — Fix before merge:
  (none)

PRIORITY 3 — Address when convenient:
  [PR DESC] Title ~84 chars (limit 70) — the trailing "(#334, #329)" pushes it over.
    Non-blocking; will set a trimmed squash title at merge.

Checked, OK:
  - Scope: 1 commit, fix(payments) — #334 + #329 both harden the SAME bank_transfer webhook.
    One coherent concern, not unrelated intents. ✓
  - Size: 118 net lines, 7 files — well under thresholds. ✓
  - Commit: conventional, body explains WHY (both fixes). ✓
  - Negative space: VIETQR_ACCOUNT_NUMBER already exists (not a new env var); no new route/dep/
    external call; verifyWebhook opts is optional. ✓
  - Rollback: no migration / payment-mutation / destructive op — a plain code revert undoes it. ✓
  - PR desc: ## Summary + ## Test plan present, non-empty, references #334/#329. ✓

SUMMARY: 0 P1, 0 P2, 1 P3

RECOMMENDED NEXT STEPS:
  → Proceed to /security-review-deep + /architect-review; trim the squash title at merge.
