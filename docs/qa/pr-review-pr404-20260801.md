# PR REVIEW — PR #404 "fix(theme): light-orange page field so white cards separate from it" @ `ef0fbacd`

**Base:** `master` · **Head:** `feat/page-field-orange` · **State:** open, ready
**Diff scope:** 1 file, +29 / −9, 1 commit · **CI:** 13/13 · **Date:** 2026-08-01

Shape-level pass. Line-level findings are in `docs/qa/code-review-pr404-20260801.md`.

---

## PRIORITY 1 / PRIORITY 2

None.

---

## PRIORITY 3

### [PR DESC] Missing the two canonical section headings
`## What` and `## Verification` instead of `## Summary` and `## Test plan`. Semantically both are
present; this is naming only. Same finding as #399 — worth a house style decision rather than a
per-PR note. Title is 62 chars, under the 70 limit.

### [PR DESC] The body repeats the mis-scoped contrast floor
> `text-primary` 3.27 (graphics floor 3.0)

`text-primary` is used as link text at 119 call sites, where the applicable floor is 4.5 (WCAG
1.4.3), not the 3.0 non-text floor. See the P2 in the code review. Noted here because on
squash-merge this body becomes the permanent commit message, so the wrong floor gets recorded as
the justification.

---

## Verified clean

- **Scope discipline — exemplary.** One intent (`fix(theme)`), one file, one commit. Net +20 lines.
  Nothing here is doing two jobs.
- **Commit message** is conventional with a 2,026-character body explaining *why*.
- **Negative space — nothing missing.** No schema, migration, env var, route handler, feature flag,
  cron, dependency, or external call in the diff, so every companion-artifact trigger is
  inapplicable rather than unmet. `.dark` is explicitly untouched and the body says so.
- **No irreversible operation.** Rollback is `git revert`; there is no data-shaped consequence.
- **The body records a rejected alternative and why** — the pure-white field, with the measurement
  that killed it (card-vs-page collapses to 1.00 because `--card` is also white). That is the
  difference between a decision and a preference, and it stops the option being re-proposed.
- **The body corrects a stale source rather than quoting it.** It notes `docs/design/*` cite
  `#f54a00`/`#ca3500` while the live tokens resolve to `#f05100`/`#c53c00`, and states the
  measurements come from the live values. That is exactly the discipline `CLAUDE.md` 2026-07-30
  asks for.
- **Verification applied this repo's own hard-won lesson.** The body states all **seven** token
  values were checked individually against the served stylesheet, citing the 2026-07-18 +
  2026-07-31 partial-rescan entries — where three tokens updated and a fourth silently did not.
  Checking every token rather than a sample is the correct response to that entry.
- **Scope of blast radius is stated, not discovered.** `:root` tokens are site-wide, so op and
  admin move too; the body says so and gives the reason.

## Not reviewed

- Whether the served pixel values reported in the body are current — that would require a browser
  measurement, which `CLAUDE.md` (2026-08-01) says is untrustworthy after a branch switch until
  `.next/dev/cache` is cleared. The token arithmetic was verified from source instead, in the code
  review.

---

```
SUMMARY: 0 P1, 0 P2, 2 P3
```

## RECOMMENDED NEXT STEPS

1. Nothing blocks merge.
2. Fix the `graphics floor 3.0` framing in the body before squash-merge if convenient — it is one
   line and it becomes permanent history.
3. The `text-primary` link-contrast issue belongs in its own issue; it is global and predates this PR.
