# PR REVIEW — PR #407 "chore(tourism): two guards so generated data cannot be pushed" @ `bd3ef854`

**Base:** `master` (`def1ad8`) · **Head:** `chore/tourism-output-guard` · **State:** open, ready
**Diff scope:** 11 files, +508 / −9, 2 commits · **Date:** 2026-08-01

Shape-level pass. Line-level findings are in `docs/qa/code-review-pr407-20260801.md`.

---

## PRIORITY 1 / PRIORITY 2

None.

---

## PRIORITY 3

### [SCOPE] `CLAUDE.md` carries an entry about a mistake made *while building this*, not about the change

The Mistake-Log entry records that I tested the write-guard by invoking the writers, so three of them
wrote and one regenerated the parity anchor. That is a process lesson, not a description of what
this diff does — a purist would split it.

Keeping it here deliberately: the project rule is that a mistake is logged *immediately*, the lesson
is specifically about the guard being added in this PR, and the fix it points at (`duoc_phep` is
pure and should have been the thing tested) is code in this same diff. Splitting it would separate
the lesson from its subject. Noted so the choice is visible rather than accidental.

### [PR DESC] Section headings are `## Summary` / `## Test plan` — correct

Unlike #399/#403/#404, which all used narrative headings. No finding; recorded because it breaks the
run of three and is worth keeping.

---

## Verified clean

- **Scope is one thing, and the second commit is a review fix rather than new work.** Commit 1 adds
  both guards; commit 2 is the P2 from self-review (asserting every permitted root is gitignored).
  That is the intended shape for a review round — the fix is a separate, traceable commit.
- **Commit messages carry 3,463 and 1,069-character bodies** explaining *why*, both conventional
  form, no `wip`/`fixup`.
- **Size is proportionate.** +508 across 11 files, of which ~180 lines is the new module, ~150 the
  test, ~60 the G8 check, and 5 one-line call sites. The remainder is the review report and the
  `CLAUDE.md` entry. Well inside every threshold.
- **Negative space — nothing missing.** No schema, migration, env var, route handler, feature flag,
  cron, dependency, or external API call. Every companion-artifact trigger is inapplicable rather
  than unmet.
- **The new code has tests, and they are wired.** `test_duong_dan_ra.py` is added in the same commit
  as `duong_dan_ra.py` and registered in CI job 9 in the same diff — not left as an orphan the way
  `test_xep_hang.py` was before #399 wired it. This is the one negative-space trigger that could
  have fired here and does not.
- **No irreversible operation.** Rollback is `git revert`; the guards only refuse writes and fail a
  CI check. Reverting restores the prior (permissive) behaviour with no data consequence.
- **Both guards were proven to fire before being trusted**, and the PR body says so with the method,
  not just the claim: each of G8's four checks staged with an isolating violation, check 1 re-run
  with a non-guide `.json` so it could not have been passing on checks 2/3, and the new drift
  assertion verified by injecting a non-ignored root and watching it fail. This is the
  `import-x/no-cycle` lesson applied without being asked.
- **The PR body states its own boundary.** The 6 `sys.argv[1]` sweeps and ~25 `RAW`-joining writers
  are named as out of scope with the reason (G8 still covers them at commit time), rather than
  quietly omitted.
- **The body discloses a side effect the author caused** — the regenerated parity anchor, its mtime
  skew, why the content is nonetheless unchanged (`guide_data.json` still stamped 15:10:11), and an
  explicit refusal to `touch` the timestamp back. That last point matters: silencing a freshness
  guard by faking its input is the repair that makes the next reader's evidence wrong.
- **No numbers asserted from memory.** The nine-path committability table in the body is the
  measured output of `git check-ignore`, re-derivable in one command.

## Not reviewed

- Whether the Vietnamese comment register matches the surrounding files (it does, by inspection, but
  that is a style judgement).
- Runtime cost of `git check-ignore` shelling out once per allowed root in the test — six
  subprocesses, only in CI, negligible against the 6.6 s pre-commit scanner already accepted.

---

```
SUMMARY: 0 P1, 0 P2, 2 P3
```

## RECOMMENDED NEXT STEPS

1. Nothing blocks merge.
2. Watch CI specifically for the **Greppable Invariants** and **Python Syntax** jobs — the first now
   runs G8 for the first time against a real checkout, the second now runs the new guard tests.
