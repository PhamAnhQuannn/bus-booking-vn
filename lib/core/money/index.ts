/**
 * [SYS20] lib/core/money — currency primitive (placeholder).
 *
 * Will hold minor-unit (VND) money math, BigInt fee/payout rounding, and formatting helpers
 * (see Mistake Log Issue 016: currency math runs in the BigInt domain). Imported BY domains;
 * imports NO domain. Real primitives migrate here in a later wave — empty barrel for now.
 */

export {};
