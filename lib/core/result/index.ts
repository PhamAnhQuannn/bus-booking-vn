/**
 * [SYS20] lib/core/result — discriminated-result primitive (placeholder).
 *
 * Will hold the `{ ok: true, ... } | { ok: false, ... }` result type used for idempotent
 * operations (see Mistake Log Issue 013: discriminated result over thrown sentinel). Imported
 * BY domains; imports NO domain. Real primitives migrate here in a later wave — empty for now.
 */

export {};
