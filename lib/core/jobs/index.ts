/**
 * [SYS20] lib/core/jobs — background-job primitive (placeholder, SYS10).
 *
 * Will hold the job/cron-handler contract shared by `app/api/cron/**` and the future
 * `worker/` entrypoint (same handlers, two callers). Imported BY domains; imports NO domain.
 * Real primitives migrate here in a later wave — empty barrel for now.
 */

export {};
