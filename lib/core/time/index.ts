/**
 * [SYS20] lib/core/time — time/timezone primitive (placeholder).
 *
 * Will hold UTC+7 (Asia/Ho_Chi_Minh) service-date helpers, window-overlap math, and clock
 * abstraction (see Mistake Log Issue 014: service-date filters are timezone-aware). Imported
 * BY domains; imports NO domain. Real primitives migrate here in a later wave — empty for now.
 */

export {};
