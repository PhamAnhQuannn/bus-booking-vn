/**
 * [SYS20] lib/core/errors — shared error primitive (placeholder).
 *
 * Will hold the base tagged-error union machinery + HTTP-status mapping shared by domain
 * error modules. Imported BY domains; imports NO domain. Real primitives migrate here in a
 * later wave — empty barrel for now.
 */

export {};
