/**
 * [SYS20] lib/core/id — identifier primitive (placeholder).
 *
 * Will hold ID/ref generation (cuid/uuidv7, booking-ref formatting + exported regex). Imported
 * BY domains; imports NO domain. Real primitives migrate here in a later wave — empty for now.
 */

export {};
