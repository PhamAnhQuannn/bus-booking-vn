# Staging Docker Compose — FPT Cloud / self-hosted backup deployment.
# Primary staging uses Vercel Preview deployments (ADR-020 D11).
services:
  # NOTE: Run database migrations before starting this stack.
  # One-off command: docker compose -f docker-compose.staging.yml run --rm app pnpm prisma migrate deploy
  app:
    build: .
    env_file: .env.staging
    environment:
      DATABASE_URL: "postgresql://bbvn:${POSTGRES_PASSWORD}@pgbouncer:6432/bbvn_staging"
      DIRECT_URL: "postgresql://bbvn:${POSTGRES_PASSWORD}@postgres:5432/bbvn_staging"
      NODE_ENV: production
    ports:
      - '3001:3000'
    depends_on:
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks: [internal]

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: bbvn
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: bbvn_staging
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U bbvn -d bbvn_staging']
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks: [internal]

  pgbouncer:
    image: edoburu/pgbouncer:1.22.1
    environment:
      DATABASE_URL: "postgres://bbvn:${POSTGRES_PASSWORD}@postgres:5432/bbvn_staging"
      POOL_MODE: transaction
      MAX_CLIENT_CONN: 200
      PGBOUNCER_DEFAULT_POOL_SIZE: 30
      AUTH_TYPE: scram-sha-256
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -p 6432 -U bbvn"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks: [internal]

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD required}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', '-a', '${REDIS_PASSWORD:?REDIS_PASSWORD required}', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks: [internal]

  cron:
    image: aptible/supercronic:latest
    volumes:
      - ./deploy/crontab/crontab:/etc/supercronic/crontab:ro
    environment:
      TZ: Asia/Ho_Chi_Minh
      CRON_SECRET: ${CRON_SECRET}
    entrypoint: ["/usr/local/bin/supercronic", "/etc/supercronic/crontab"]
    depends_on:
      app:
        condition: service_started
    restart: unless-stopped
    networks: [internal]

volumes:
  postgres_data:
  redis_data:

networks:
  internal:
    driver: bridge
    internal: true
